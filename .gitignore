# Build outputs (`make build` -> bin/straddle; stray root builds)
/straddle
/bin/
/gen-endpoint

# Build / release artifacts
/build/
/dist/

# Local SQLite store + cache
*.db
*.db-shm
*.db-wal
demo/out/

# npm wrapper: vendored binary fetched at install time
/npm/vendor/
