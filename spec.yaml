openapi: 3.1.0
info:
  title: Straddle API
  version: 1.0.1
  description: >-
    # Introduction


    The Straddle API uses resource URLs, standard HTTP methods, and JSON request
    and response bodies.



    # Environments


    Sandbox and Production use separate base URLs and API keys. Use Sandbox for
    testing and development. Use Production for live transactions.


    | Environment | Base URL                      | Purpose                 |

    |-------------|-------------------------------|------------------------|

    | Sandbox     | `https://sandbox.straddle.com` | Testing and development |

    | Production  | `https://production.straddle.com` | Live
    transactions       |


    Use the API key issued for the request's environment. An API key from
    another environment does not authenticate the request.



    # Authentication


    Straddle authenticates API requests with JWT bearer tokens. Send your secret
    API key in the `Authorization` header:


    ```bash

    curl https://sandbox.straddle.com/v1/customers \
      -H "Authorization: Bearer YOUR_SECRET_API_KEY"
    ```



    # Idempotent requests


    Use idempotency to retry a request without performing the same operation
    twice. Send a unique `Idempotency-Key` header with the request.


    After Straddle processes a request successfully, an identical request with
    the same key returns the original status code, headers, and body without
    repeating the operation.



    ### Request headers


    Send a unique string of 10 to 40 characters in the `Idempotency-Key` header.


    | Header | Type | Description |

    |--------|------|-------------|

    | `Idempotency-Key` | string | A unique identifier for the request. The
    value must contain 10 to 40 characters. |


    ### Response headers


    A successfully replayed request returns the original HTTP status code,
    response headers, and body. The response also includes:


    | Header | Type | Description |

    |--------|------|-------------|

    | `Idempotent-Replayed` | boolean | `true` when the response replays a
    previously successful request. |


    ### Idempotency errors


    The API returns these errors for invalid or conflicting idempotency keys:


    | Status Code | Error Type | Description |

    |-------------|------------|-------------|

    | **400** | Bad Request | The `Idempotency-Key` value is shorter than 10
    characters or longer than 40 characters. |

    | **409** | Conflict | The key was used for a different request, or an
    identical request with the key is still in progress. |


    ### Key reused for a different request


    A request returns `409 Conflict` when its `Idempotency-Key` was used for a
    request with a different path, method, or body.


    ```json

    {
      "status": 409,
      "type": "/conflict",
      "title": "Conflict",
      "detail": "Idempotency key already used for a different request."
    }

    ```


    ### Identical request still in progress


    An identical request returns `409 Conflict` when another request with the
    same `Idempotency-Key` is still in progress.


    ```json

    {
      "status": 409,
      "type": "/conflict",
      "title": "Conflict",
      "detail": "Previous identical request currently in progress."
    }

    ```


    ### Invalid key length


    A request returns `400 Bad Request` when its `Idempotency-Key` is shorter
    than 10 characters or longer than 40 characters.


    ```json

    {
      "status": 400,
      "type": "/bad-request",
      "title": "Bad Request",
      "detail": "The Idempotency-Key header is invalid. It must be between 10 and 40 characters long."
    }

    ```



    ### Example request


    ```bash

    curl -X POST https://sandbox.straddle.com/v1/charges \

    -H "Authorization: Bearer YOUR_API_KEY" \

    -H "Content-Type: application/json" \

    -H "Idempotency-Key: unique-client-key-7890" \

    -d '{
      "amount": 100.00,
      "currency": "USD"
    }'

    ```


    Idempotency applies only to authenticated `POST`, `PUT`, `PATCH`, and
    `DELETE` requests.



    # Response structure


    API responses use a JSON envelope. Every envelope has `meta` and
    `response_type`. Other fields depend on the response type.


    - **Field names**
      - Fields use `snake_case`.
      - Resource identifiers use UUIDs.

    - **`response_type`**
      - `"object"` means `data` contains one JSON object.
      - `"array"` means `data` contains an array of JSON objects.
      - `"error"` means `error` contains the error details.
      - `"none"` means the response contains no data.

    - **`data`**
      - Successful object and array responses contain the requested resource data.

    - **`error`**
      - Error responses contain the error details.

    - **`meta`**
      - `api_request_id` is the unique identifier for the API request.
      - `api_request_timestamp` is the ISO 8601 timestamp for the API request.
      - Paginated array responses also include `page_number`, `page_size`, `total_items`, `sort_order`, and `sort_by`.

    ### Examples


    #### Object response


    ```json

    {
      "meta": {
        "api_request_id": "3a2b1c4d-0e6f-4a88-9876-123456abcdef",
        "api_request_timestamp": "2023-11-07T05:31:56Z"
      },
      "response_type": "object",
      "data": {
        "id": "a1b2c3d4-e5f6-7a8b-9c0d-1e2f3a4b5c6d",
        "name": "John Doe",
        "email": "john.doe@example.com",
        "dob": "1990-01-01",
        "address": {
          "street": "123 Main St",
          "city": "Springfield",
          "state": "IL",
          "postal_code": "62701"
        }
      }
    }

    ```


    #### Paginated array response


    ```json

    {
      "meta": {
        "api_request_id": "3c90c3cc-0d44-4b50-8888-8dd25736052a",
        "api_request_timestamp": "2023-11-07T05:31:56Z",
        "page_number": 2,
        "page_size": 50,
        "total_items": 500,
        "sort_order": "asc",
        "sort_by": "name"
      },
      "response_type": "array",
      "data": [
        {
          "id": "c1b1b1b1-1b1b-1b1b-1b1b-1b1b1b1b1b1b",
          "name": "John Doe",
          "email": "john.doe@example.com"
        },
        {
          "id": "a9b1b1b1-1b1b-1b1b-1b1b-1b1b1b1b1b1b",
          "name": "Steven Martin",
          "email": "steven.martin@example.com"
        }
      ]
    }

    ```


    #### Error response


    ```json

    {
      "meta": {
        "api_request_id": "f1b1b1b1-1b1b-1b1b-1b1b-1b1b1b1b1b1b"
      },
      "response_type": "error",
      "error": {
        "status": 400,
        "type": "/field_validation",
        "title": "Invalid Input Data",
        "detail": "The request contains invalid field values.",
        "items": [
          {
            "reference": "customer.email",
            "detail": "Email address must be unique."
          }
        ]
      }
    }

    ```



    # Metadata


    The `metadata` field stores user-defined key-value pairs on supported
    Straddle objects.


    `metadata` contains custom data that you provide. It is separate from the
    response `meta` object, which contains information about the API request.


    ### Add metadata


    Include `metadata` when you create or update an object that supports the
    field.


    #### Customer request example


    This customer request includes two metadata entries:


    ```json

    {
      "name": "John Doe",
      "type": "individual",
      "email": "john@example.com",
      "address": {
        "address1": "123 Main St",
        "address2": null,
        "type": "residential",
        "city": "Springfield",
        "state": "IL",
        "zip": "62701"
      },
      "phone": "+1234567890",
      "external_id": "CUS-123",
      "device": {
        "ip_address": "192.168.1.1"
      },
      "metadata": {
        "order_id": "6735",
        "customer_group": "premium"
      }
    }

    ```



    ### Limits


    - Keys and values can contain at most 40 characters.

    - The `metadata` object can contain at most 20 entries.


    # Embedded accounts


    Platforms can send requests in the scope of an embedded account. Send the
    embedded account's UUID in the `Straddle-Account-Id` header.



    ### Send a request for an embedded account


    Include `Straddle-Account-Id` with the embedded account UUID. Authenticate
    the request with the platform's secret API key.


    #### Example charge request


    This request creates a charge in the embedded account's scope.


    ```bash

    curl --request POST \
      --url https://sandbox.straddle.com/v1/charges \
      --header 'Authorization: Bearer <your-secret-api-key>' \
      --header 'Content-Type: application/json' \
      --header 'Straddle-Account-Id: <uuid>' \
      --data '{
        "paykey": "<string>",
        "description": "<string>",
        "amount": 123,
        "currency": "<string>",
        "payment_date": "2023-12-25",
        "consent_type": "internet",
        "device": {
          "ip_address": "<string>"
        },
        "external_id": "<string>",
        "config": {
          "balance_check": "required"
        },
        "metadata": {}
      }'
    ```



    # Errors


    HTTP status codes indicate whether the API completed a request. Error
    responses include a JSON `error` object.


    Common status codes include:


    | Status Code | Type | Description |

    |------------|------|-------------|

    | **200** | OK | The request completed successfully. |

    | **400** | Bad Request | The request is invalid. |

    | **401** | Unauthorized | The request does not include a valid API key. |

    | **403** | Forbidden | The API key does not have permission to perform the
    request. |

    | **404** | Not Found | The requested resource does not exist. |

    | **409** | Conflict | The request conflicts with an existing request or
    operation. |

    | **422** | Unprocessable Entity | The request syntax is valid, but one or
    more values cannot be processed. |

    | **429** | Too Many Requests | The request exceeded a rate limit. |

    | **500, 502, 503, 504** | Server Error | Straddle could not complete the
    request because of a server error. |



    ### Error response structure


    Error responses use the standard response envelope. The `error` object has
    these fields:


    | Attribute | Type | Description |

    |-----------|------|-------------|

    | `status` | integer | HTTP status code for the error. |

    | `type` | string | Identifier for the error type. |

    | `title` | string | Short summary of the error. |

    | `detail` | string | Explanation specific to this request. |

    | `items` | array | Additional error details, when available. |

    | `items[].reference` | string | Request field or error code associated with
    the detail. |

    | `items[].detail` | string | Explanation of the error detail. |


    ### Example error response


    This example shows a field-validation error:


    ```json

    {
      "meta": {
        "api_request_id": "3a2b1c4d-0e6f-4a88-9876-123456abcdef",
        "api_request_timestamp": "2023-11-07T05:31:56Z"
      },
      "response_type": "error",
      "error": {
        "status": 400,
        "type": "validation_error",
        "title": "Invalid Input Data",
        "detail": "The request contains invalid field values.",
        "items": [
          {
            "reference": "customer.email",
            "detail": "Email address must be unique."
          }
        ]
      }
    }

    ```
  contact:
    name: Straddle
    url: https://straddle.io
servers:
  - url: https://{environment}.straddle.com
    description: Straddle API server
    variables:
      environment:
        default: sandbox
        enum:
          - production
          - sandbox
        description: Use sandbox for testing and production for live transactions.
security:
  - Bearer: []
tags:
  - name: customers
    description: Customers are individuals or businesses that send or receive
      payments through your integration.
    x-displayName: Customers
  - name: bridge
    description: Bridge connects customer bank accounts and creates paykeys from
      supported provider tokens or bank account details.
    x-displayName: Bridge
  - name: paykeys
    description: A paykey links a verified customer to a bank account without
      exposing bank account details. Use a paykey to create charges and payouts.
    x-displayName: Paykeys
  - name: charges
    description: Charges debit a customer's bank account through a paykey.
    x-displayName: Charges
  - name: payouts
    description: Payouts send money to a customer's bank account through a paykey.
    x-displayName: Payouts
  - name: fundingEvents
    description: Funding events group charge and payout activity into transfers
      between Straddle and your linked bank account.
    x-displayName: Funding events
  - name: payments
    description: Payments provide a combined view of charges and payouts.
    x-displayName: Payments
  - name: organizations
    description: Organizations group related Straddle accounts.
    x-displayName: Organizations
  - name: accounts
    description: Accounts represent businesses that use Straddle through a platform.
    x-displayName: Accounts
  - name: representatives
    description: Representatives are people associated with a business account for
      ownership, control, or authorization purposes.
    x-displayName: Representatives
  - name: capabilityRequests
    description: Capability requests change the payment, customer, and consent types
      available to an account.
    x-displayName: Capabilities
  - name: linkedBankAccounts
    description: Linked bank accounts connect external bank accounts to an account
      or platform for charges, payouts, or billing.
    x-displayName: Linked bank accounts
  - name: accountSettings
    x-displayName: Account settings
    description: Account settings define payment limits, capabilities, statement
      details, and policy controls for an account.
x-tagGroups:
  - name: Pay by Bank
    tags:
      - customers
      - bridge
      - paykeys
      - charges
      - payouts
      - fundingEvents
      - payments
  - name: Embed
    tags:
      - organizations
      - accounts
      - representatives
      - linkedBankAccounts
      - capabilityRequests
      - accountSettings
paths:
  /v1/accounts/{account_id}:
    get:
      tags:
        - accounts
      summary: Get an account
      description: Returns the account with the specified ID.
      operationId: getAccount
      parameters:
        - name: account_id
          in: path
          description: The ID of the account.
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/AccountResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/AccountResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/AccountResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
    put:
      tags:
        - accounts
      summary: Update an account
      description: Updates an account's business profile, metadata, and external ID,
        then returns the account.
      operationId: updateAccount
      parameters:
        - name: account_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: The ID of the account.
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateAccountRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/UpdateAccountRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/UpdateAccountRequest"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/AccountResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/AccountResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/AccountResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
  /v1/accounts:
    post:
      tags:
        - accounts
      summary: Create an account
      description: Creates a business account in the specified organization and
        returns the account.
      operationId: createAccount
      parameters:
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAccountRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/CreateAccountRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/CreateAccountRequest"
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/AccountResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/AccountResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/AccountResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
    get:
      tags:
        - accounts
      summary: List accounts
      description: Returns a paginated list of accounts for your platform. Filter the
        list by status, type, external ID, or text search.
      operationId: listAccounts
      parameters:
        - name: page_number
          in: query
          description: Page number. Defaults to `1`.
          required: false
          schema:
            type: integer
            format: int32
            default: 1
        - name: page_size
          in: query
          description: Number of results per page. Defaults to `100`. Maximum `1000`.
          required: false
          schema:
            type: integer
            format: int32
            default: 100
        - name: sort_by
          in: query
          description: Field used to sort results. Defaults to `id`.
          required: false
          schema:
            type: string
            default: id
        - name: sort_order
          in: query
          description: Sort direction. Defaults to `asc`.
          required: false
          schema:
            enum:
              - asc
              - desc
            type: string
            default: asc
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - name: search_text
          in: query
          schema:
            type: string
          description: Text to search for across account fields.
        - name: status
          in: query
          schema:
            enum:
              - created
              - onboarding
              - active
              - rejected
              - inactive
            type: string
          description: Account status to return.
        - name: type
          in: query
          schema:
            enum:
              - business
            type: string
          description: Account type to return.
        - name: external_id
          in: query
          schema:
            type: string
          description: Your external ID for the account.
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/AccountListResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/AccountListResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/AccountListResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
  /v1/accounts/{account_id}/onboard:
    post:
      tags:
        - accounts
      summary: Onboard an account
      description: Starts onboarding and records the account's acceptance of
        Straddle's Terms of Service. The account must have at least one
        representative and one linked bank account. This operation also moves
        all associated representatives and linked bank accounts to `onboarding`.
      operationId: onboardAccount
      parameters:
        - name: account_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: The ID of the account.
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OnboardAccountRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/OnboardAccountRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/OnboardAccountRequest"
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/AccountResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/AccountResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/AccountResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
  /v1/accounts/{account_id}/capability_requests:
    post:
      tags:
        - capabilityRequests
      summary: Create capability requests
      description: Creates one or more capability requests for an account and returns
        the resulting requests.
      operationId: createCapabilityRequest
      parameters:
        - name: account_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: The ID of the account.
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RequestCapabilityRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/RequestCapabilityRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/RequestCapabilityRequest"
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/CapabilityRequestListResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/CapabilityRequestListResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/CapabilityRequestListResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
    get:
      tags:
        - capabilityRequests
      summary: List capability requests
      description: Returns a paginated list of capability requests for an account.
        Filter the list by capability type, category, or status.
      operationId: listCapabilityRequests
      parameters:
        - name: account_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: The ID of the account.
        - name: type
          in: query
          description: Capability type to return.
          schema:
            enum:
              - charges
              - payouts
              - individuals
              - businesses
              - signed_agreement
              - internet
            type: string
        - name: category
          in: query
          description: Capability category to return.
          schema:
            enum:
              - payment_type
              - customer_type
              - consent_type
            type: string
        - name: status
          in: query
          description: Capability request status to return.
          schema:
            enum:
              - active
              - inactive
              - in_review
              - rejected
            type: string
        - name: page_number
          in: query
          description: Page number. Defaults to `1`.
          required: false
          schema:
            type: integer
            format: int32
            default: 1
        - name: page_size
          in: query
          description: Number of results per page. Defaults to `100`. Maximum `1000`.
          required: false
          schema:
            type: integer
            format: int32
            default: 100
        - name: sort_by
          in: query
          description: Field used to sort results. Defaults to `id`.
          required: false
          schema:
            type: string
            default: id
        - name: sort_order
          in: query
          description: Sort direction. Defaults to `asc`.
          required: false
          schema:
            enum:
              - asc
              - desc
            type: string
            default: asc
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/CapabilityRequestListResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/CapabilityRequestListResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/CapabilityRequestListResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
  /v1/accounts/{account_id}/simulate:
    post:
      tags:
        - accounts
      summary: Simulate status transitions for a sandbox account
      description: Simulates an account status transition to `onboarding` or `active`
        in the sandbox and returns the account.
      parameters:
        - name: account_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: The ID of the account.
        - name: final_status
          in: query
          schema:
            enum:
              - onboarding
              - active
            type: string
          description: Final account status to produce in the sandbox simulation.
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/AccountResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/AccountResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/AccountResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
      operationId: simulateAccountOnboarding
  /v1/linked_bank_accounts:
    post:
      tags:
        - linkedBankAccounts
      summary: Create a linked bank account
      description: Creates a linked bank account for an account or platform, assigns
        its payment purposes, and returns the linked bank account.
      operationId: createLinkedBankAccount
      parameters:
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLinkedBankAccountRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/CreateLinkedBankAccountRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/CreateLinkedBankAccountRequest"
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/LinkedBankAccountResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/LinkedBankAccountResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/LinkedBankAccountResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
    get:
      tags:
        - linkedBankAccounts
      summary: List linked bank accounts
      description: Returns a paginated list of linked bank accounts. Filter the list
        by account, scope, purpose, or status.
      parameters:
        - name: account_id
          in: query
          description: Account ID used to filter the results.
          schema:
            type: string
            format: uuid
        - name: page_number
          in: query
          description: Page number. Defaults to `1`.
          required: false
          schema:
            type: integer
            format: int32
            default: 1
        - name: page_size
          in: query
          description: Number of results per page. Defaults to `100`. Maximum `1000`.
          required: false
          schema:
            type: integer
            format: int32
            default: 100
        - name: sort_by
          in: query
          description: Field used to sort results. Defaults to `id`.
          required: false
          schema:
            type: string
            default: id
        - name: sort_order
          in: query
          description: Sort direction. Defaults to `asc`.
          required: false
          schema:
            enum:
              - asc
              - desc
            type: string
            default: asc
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - name: level
          in: query
          schema:
            enum:
              - account
              - platform
            type: string
          description: Scope of linked bank accounts to return.
        - name: purpose
          in: query
          description: Linked bank account purpose. Accepted values are `charges`,
            `payouts`, and `billing`.
          schema:
            enum:
              - charges
              - payouts
              - billing
            type: string
        - name: status
          in: query
          description: Linked bank account status. Accepted values are `created`,
            `onboarding`, `active`, `rejected`, `inactive`, and `canceled`.
          schema:
            enum:
              - created
              - onboarding
              - active
              - rejected
              - inactive
              - canceled
            type: string
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/LinkedBankAccountListResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/LinkedBankAccountListResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/LinkedBankAccountListResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
      operationId: listLinkedBankAccounts
  /v1/linked_bank_accounts/{linked_bank_account_id}:
    put:
      tags:
        - linkedBankAccounts
      summary: Update a linked bank account
      description: Updates bank account details and metadata, then returns the linked
        bank account. The linked bank account must have status `created`, or
        status `onboarding` with `status_detail.reason` set to `stuck`.
      operationId: updateLinkedBankAccount
      parameters:
        - name: linked_bank_account_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: The ID of the linked bank account.
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLinkedBankAccountRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/UpdateLinkedBankAccountRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/UpdateLinkedBankAccountRequest"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/LinkedBankAccountResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/LinkedBankAccountResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/LinkedBankAccountResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
    get:
      tags:
        - linkedBankAccounts
      summary: Get a linked bank account
      description: Returns the linked bank account with the specified ID. The response
        masks the account number.
      operationId: getLinkedBankAccount
      parameters:
        - name: linked_bank_account_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: The ID of the linked bank account.
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/LinkedBankAccountResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/LinkedBankAccountResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/LinkedBankAccountResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
  /v1/linked_bank_accounts/{linked_bank_account_id}/unmask:
    get:
      tags:
        - linkedBankAccounts
      summary: Get an unmasked linked bank account
      description: Returns the linked bank account with the specified ID without
        masking its account number. This endpoint is available only when
        Straddle enables data unmasking for the account.
      operationId: getUnmaskedLinkedBankAccount
      parameters:
        - name: linked_bank_account_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: The ID of the linked bank account.
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/UnmaskedLinkedBankAccountResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/UnmaskedLinkedBankAccountResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/UnmaskedLinkedBankAccountResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
  /v1/organizations:
    post:
      tags:
        - organizations
      summary: Create an organization
      description: Creates an organization for your platform and returns it.
        Organizations group related accounts and users.
      operationId: createOrganization
      parameters:
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOrganizationRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/CreateOrganizationRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/CreateOrganizationRequest"
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/OrganizationResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/OrganizationResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
    get:
      tags:
        - organizations
      summary: List organizations
      description: Returns a paginated list of organizations for your platform. Filter
        the list by name or external ID.
      operationId: listOrganizations
      parameters:
        - name: name
          in: query
          description: Organization name. Supports partial matches.
          schema:
            type: string
        - name: external_id
          in: query
          description: Your external ID for the organization.
          schema:
            type: string
        - name: page_number
          in: query
          description: Page number. Defaults to `1`.
          required: false
          schema:
            type: integer
            format: int32
            default: 1
        - name: page_size
          in: query
          description: Number of results per page. Defaults to `100`. Maximum `1000`.
          required: false
          schema:
            type: integer
            format: int32
            default: 100
        - name: sort_by
          in: query
          description: Field used to sort results. Defaults to `id`.
          required: false
          schema:
            type: string
            default: id
        - name: sort_order
          in: query
          description: Sort direction. Defaults to `asc`.
          required: false
          schema:
            enum:
              - asc
              - desc
            type: string
            default: asc
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/OrganizationListResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationListResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/OrganizationListResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
  /v1/representatives:
    post:
      tags:
        - representatives
      summary: Create a representative
      description: Creates a representative for an account and returns the
        representative. Relationship fields identify primary representatives,
        control persons, and owners.
      operationId: createRepresentative
      parameters:
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRepresentativeRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/CreateRepresentativeRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/CreateRepresentativeRequest"
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/RepresentativeResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/RepresentativeResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/RepresentativeResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
    get:
      tags:
        - representatives
      summary: List representatives
      operationId: listRepresentatives
      description: Returns a paginated list of representatives. Filter the list by
        account, organization, platform, or scope.
      parameters:
        - name: account_id
          in: query
          description: Account ID used to filter the results.
          schema:
            type: string
            format: uuid
        - name: page_number
          in: query
          description: Page number. Defaults to `1`.
          required: false
          schema:
            type: integer
            format: int32
            default: 1
        - name: page_size
          in: query
          description: Number of results per page. Defaults to `100`. Maximum `1000`.
          required: false
          schema:
            type: integer
            format: int32
            default: 100
        - name: sort_by
          in: query
          description: Field used to sort results. Defaults to `id`.
          required: false
          schema:
            type: string
            default: id
        - name: sort_order
          in: query
          description: Sort direction. Defaults to `asc`.
          required: false
          schema:
            enum:
              - asc
              - desc
            type: string
            default: asc
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - name: platform_id
          in: query
          schema:
            type: string
            format: uuid
          description: Platform ID used to filter the results.
        - name: organization_id
          in: query
          schema:
            type: string
            format: uuid
          description: Organization ID used to filter the results.
        - name: level
          in: query
          schema:
            enum:
              - account
              - platform
            type: string
          description: Scope of representatives to return.
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/RepresentativeListResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/RepresentativeListResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/RepresentativeListResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
  /v1/representatives/{representative_id}:
    put:
      tags:
        - representatives
      summary: Update a representative
      operationId: updateRepresentative
      description: Updates a representative's personal, contact, relationship,
        external ID, and metadata fields, then returns the representative.
      parameters:
        - name: representative_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: The ID of the representative.
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRepresentativeRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/UpdateRepresentativeRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/UpdateRepresentativeRequest"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/RepresentativeResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/RepresentativeResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/RepresentativeResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
    get:
      tags:
        - representatives
      summary: Get a representative
      operationId: getRepresentative
      description: Returns the representative with the specified ID.
      parameters:
        - name: representative_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: The ID of the representative.
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/RepresentativeResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/RepresentativeResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/RepresentativeResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
  /v1/bridge/bank_account:
    post:
      tags:
        - bridge
      summary: Create a paykey from bank account details
      operationId: createBankAccountPaykey
      description: Creates a paykey from a routing number, account number, and account type.
      parameters:
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateBankAccountPaykeyRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/CreateBankAccountPaykeyRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/CreateBankAccountPaykeyRequest"
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/bridge/plaid:
    post:
      tags:
        - bridge
      summary: Create a paykey from a Plaid token
      operationId: createPlaidPaykey
      description: Creates a paykey from a Plaid processor token.
      parameters:
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePlaidPaykeyRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/CreatePlaidPaykeyRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/CreatePlaidPaykeyRequest"
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/bridge/initialize:
    post:
      tags:
        - bridge
      summary: Create a Bridge widget session token
      description: Creates a session token for the Bridge widget.
      operationId: createBridgeToken
      parameters:
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateBridgeTokenRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/CreateBridgeTokenRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/CreateBridgeTokenRequest"
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/BridgeTokenResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/BridgeTokenResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/BridgeTokenResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/customers/{id}:
    get:
      tags:
        - customers
      summary: Get a customer
      description: Returns a customer by `id`.
      operationId: getCustomer
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the customer.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/CustomerResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/CustomerResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      tags:
        - customers
      summary: Update a customer
      description: Updates an existing customer's profile, status, and metadata.
      operationId: updateCustomer
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the customer.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateCustomerRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/UpdateCustomerRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/UpdateCustomerRequest"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/CustomerResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/CustomerResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      tags:
        - customers
      summary: Delete a customer
      description: Permanently deletes a customer record. The deletion cannot be
        undone. Use this endpoint only to meet regulatory or privacy
        requirements.
      operationId: deleteCustomer
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the customer.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/CustomerResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/CustomerResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/customers:
    get:
      tags:
        - customers
      summary: List customers
      description: Returns a paginated list of customers for the account. Optional
        query parameters filter, search, and sort the results.
      operationId: listCustomers
      parameters:
        - name: page_number
          required: false
          in: query
          description: Page number for paginated results. Starts at 1.
          schema:
            type: integer
            format: int32
            default: 1
        - name: page_size
          required: false
          in: query
          description: "Number of results per page. Maximum: 1000."
          schema:
            type: integer
            format: int32
            default: 100
        - name: sort_by
          required: false
          in: query
          schema:
            $ref: "#/components/schemas/CustomerSortBy"
          description: Field used to sort the results.
        - name: sort_order
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/SortOrder"
          description: Order in which to sort the results.
        - name: created_from
          in: query
          required: false
          description: Start date for filtering by `created_at` date.
          schema:
            type: string
            format: date-time
        - name: created_to
          in: query
          description: End date for filtering by `created_at` date.
          schema:
            type: string
            format: date-time
        - name: name
          in: query
          description: Filter customers by `name` (partial match).
          schema:
            type: string
        - name: external_id
          in: query
          description: Filter by your system's `external_id`.
          schema:
            type: string
        - name: email
          in: query
          description: Filter customers by `email` address.
          schema:
            type: string
        - name: status
          in: query
          description: Filter customers by their current `status`.
          schema:
            type: array
            items:
              $ref: "#/components/schemas/CustomerStatus"
        - name: search_text
          in: query
          description: General search term to filter customers.
          schema:
            type: string
        - name: types
          in: query
          description: Filter by customer type `individual` or `business`.
          schema:
            type: array
            items:
              $ref: "#/components/schemas/CustomerType"
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/CustomerSummaryListResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerSummaryListResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/CustomerSummaryListResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      tags:
        - customers
      summary: Create a customer
      description: Creates a customer and starts identity, fraud, and risk assessments.
      operationId: createCustomer
      parameters:
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCustomerRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/CreateCustomerRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/CreateCustomerRequest"
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/CustomerResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/CustomerResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/customers/{id}/unmasked:
    get:
      tags:
        - customers
      summary: Get an unmasked customer
      description: Returns unmasked details for a customer, including personally
        identifiable information. Straddle must enable this endpoint for your
        account. Use this endpoint only when unmasked data is necessary.
      operationId: getUnmaskedCustomer
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the customer.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/UnmaskedCustomerResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/UnmaskedCustomerResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/UnmaskedCustomerResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/customers/{id}/review:
    get:
      tags:
        - customers
      summary: Get a customer review
      description: Returns the results of a customer's identity and fraud review. The
        response includes decisions, risk and correlation scores, reason codes,
        watchlist matches, and network alerts.
      operationId: getCustomerReview
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the customer.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/CustomerReviewResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerReviewResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/CustomerReviewResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      tags:
        - customers
      summary: Set a customer verification decision
      description: Updates the verification decision for a customer. The customer's
        current `status` must be `review`.
      operationId: setCustomerVerificationDecision
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the customer.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetCustomerVerificationDecisionRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/SetCustomerVerificationDecisionRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/SetCustomerVerificationDecisionRequest"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/CustomerResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/CustomerResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/paykeys/{id}:
    get:
      tags:
        - paykeys
      summary: Get a paykey
      description: Returns a paykey by `id`, including the masked paykey value and
        bank account details.
      operationId: getPaykey
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the paykey.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/paykeys/{id}/unmasked:
    get:
      tags:
        - paykeys
      summary: Get an unmasked paykey
      description: Returns a paykey by `id`, including the full paykey value and
        unmasked bank account details. Straddle must enable this endpoint for
        your account. Use this endpoint only when unmasked data is necessary.
      operationId: getUnmaskedPaykey
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the paykey.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/UnmaskedPaykeyResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/UnmaskedPaykeyResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/UnmaskedPaykeyResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/paykeys:
    get:
      tags:
        - paykeys
      summary: List paykeys
      description: Returns a paginated list of paykeys for the account. Optional query
        parameters filter, search, and sort the results.
      operationId: listPaykeys
      parameters:
        - name: customer_id
          in: query
          required: false
          description: Filter paykeys by related customer ID.
          schema:
            type: string
            format: uuid
        - name: page_number
          required: false
          in: query
          description: Page number for paginated results. Starts at 1.
          schema:
            type: integer
            default: 1
            format: int32
        - name: page_size
          required: false
          in: query
          description: "Number of results per page. Maximum: 1000."
          schema:
            type: integer
            format: int32
            default: 100
        - name: status
          in: query
          required: false
          description: Filter paykeys by their current status.
          schema:
            type: array
            items:
              $ref: "#/components/schemas/PaykeyStatus"
        - name: sort_by
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/PaykeySortBy"
          description: Field used to sort the results.
        - name: sort_order
          required: false
          in: query
          schema:
            $ref: "#/components/schemas/SortOrder"
          description: Order in which to sort the results.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - name: source
          in: query
          description: Filter paykeys by their source.
          schema:
            type: array
            items:
              $ref: "#/components/schemas/PaykeySource"
        - name: unblock_eligible
          in: query
          description: Filters paykeys by unblock eligibility. `true` returns blocked
            paykeys that are eligible because of an `R29` return and have not
            been unblocked before. `false` returns blocked paykeys that are not
            eligible.
          schema:
            type: boolean
        - name: search_text
          in: query
          description: General search term to filter paykeys.
          schema:
            type: string
        - name: created_from
          in: query
          description: Start date for filtering by creation date.
          schema:
            type: string
            format: date-time
        - name: created_to
          in: query
          description: End date for filtering by creation date.
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PaykeySummaryListResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PaykeySummaryListResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PaykeySummaryListResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/charges/{id}:
    get:
      tags:
        - charges
      summary: Get a charge
      description: Returns a charge by its unique identifier.
      operationId: getCharge
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the charge.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      tags:
        - charges
      summary: Update a charge
      operationId: updateCharge
      description: Updates the description, amount, `payment_date`, or metadata. The
        charge must have a status of `created` or `on_hold`.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the charge.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateChargeRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/UpdateChargeRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/UpdateChargeRequest"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/charges:
    post:
      tags:
        - charges
      summary: Create a charge
      operationId: createCharge
      description: Creates a charge against a customer's paykey. Straddle submits the
        charge for processing on `payment_date` unless the charge is on hold.
      parameters:
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateChargeRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/CreateChargeRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/CreateChargeRequest"
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/charges/{id}/hold:
    put:
      tags:
        - charges
      summary: Hold a charge
      description: Places a charge on hold to prevent submission for processing. The
        charge must have a status of `created` or `scheduled`.
      operationId: holdCharge
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the charge.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateChargeStatusRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/UpdateChargeStatusRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/UpdateChargeStatusRequest"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/charges/{id}/release:
    put:
      tags:
        - charges
      summary: Release a charge
      description: Releases a charge from `on_hold` and returns it to `created` for
        submission on `payment_date`.
      operationId: releaseCharge
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the charge.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateChargeStatusRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/UpdateChargeStatusRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/UpdateChargeStatusRequest"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/charges/{id}/cancel:
    put:
      tags:
        - charges
      summary: Cancel a charge
      description: Cancels a charge. The charge must have a status of `created`,
        `scheduled`, or `on_hold`.
      operationId: cancelCharge
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the charge.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateChargeStatusRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/UpdateChargeStatusRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/UpdateChargeStatusRequest"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/funding_events:
    get:
      tags:
        - fundingEvents
      summary: List funding events
      description: Returns a paginated list of funding events that match the specified
        filters.
      operationId: listFundingEvents
      parameters:
        - name: page_number
          in: query
          required: false
          description: Results page number. Starts at page 1.
          schema:
            type: integer
            format: int32
            default: 1
        - name: page_size
          in: query
          required: false
          description: "Results page size. Max value: 1000."
          schema:
            type: integer
            format: int32
            default: 100
        - name: sort_by
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/FundingEventSortBy"
            default: id
          description: Field used to sort the results.
        - name: sort_order
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/SortOrder"
            default: asc
          description: Order in which to sort the results.
        - name: created_from
          in: query
          description: Filter to funding events created on or after this date.
          schema:
            type: string
            format: date
            nullable: true
        - name: created_to
          in: query
          description: Filter to funding events created on or before this date.
          schema:
            type: string
            format: date
            nullable: true
        - name: direction
          in: query
          schema:
            $ref: "#/components/schemas/TransferDirection"
          description: Filter by transfer direction relative to the linked bank account.
        - name: event_type
          in: query
          schema:
            $ref: "#/components/schemas/FundingEventType"
          description: Filter by funding event type.
        - name: trace_number
          in: query
          description: Filter by a network trace number assigned during processing.
          schema:
            type: string
            nullable: true
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - name: search_text
          in: query
          description: Free-text search across funding event fields.
          schema:
            type: string
            nullable: true
        - name: status
          in: query
          description: Filter by funding event status.
          schema:
            type: array
            items:
              $ref: "#/components/schemas/PaymentStatus"
            nullable: true
        - name: trace_id
          in: query
          description: Filter by a network-level trace identifier assigned during
            processing.
          schema:
            type: string
            nullable: true
        - name: status_reason
          in: query
          description: Filter by the reason for the most recent status change.
          schema:
            type: array
            items:
              $ref: "#/components/schemas/PaymentStatusReason"
            nullable: true
        - name: status_source
          in: query
          description: Filter by the source of the most recent status change.
          schema:
            type: array
            items:
              $ref: "#/components/schemas/PaymentStatusSource"
            nullable: true
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/FundingEventSummaryListResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/FundingEventSummaryListResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/FundingEventSummaryListResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/funding_events/{id}:
    get:
      tags:
        - fundingEvents
      summary: Get a funding event
      description: Returns a funding event by its unique identifier, including its
        current status, status history, and linked bank account details when
        available.
      operationId: getFundingEvent
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the funding event.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/FundingEventResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/FundingEventResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/FundingEventResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/payments:
    get:
      tags:
        - payments
      summary: List payments
      description: Returns a paged list of charges and payouts that match the filters.
      operationId: listPayments
      parameters:
        - name: page_number
          required: false
          in: query
          description: Page number to return.
          schema:
            type: integer
            format: int32
            default: 1
        - name: page_size
          in: query
          required: false
          description: Number of results to return per page.
          schema:
            type: integer
            format: int32
            default: 100
        - name: sort_by
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/PaymentSortBy"
          description: Field used to sort the results.
        - name: sort_order
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/SortOrder"
          description: Order in which to sort the results.
        - name: payment_type
          in: query
          description: Filter by payment type.
          schema:
            type: array
            items:
              $ref: "#/components/schemas/PaymentType"
        - name: payment_status
          in: query
          description: Filter by payment status.
          schema:
            type: array
            items:
              $ref: "#/components/schemas/PaymentStatus"
        - name: payment_id
          in: query
          description: Filter by the payment's unique identifier.
          schema:
            type: string
            format: uuid
        - name: external_id
          in: query
          description: Filter by your external identifier for the payment.
          schema:
            type: string
        - name: customer_id
          in: query
          description: Filter by the unique identifier of the customer.
          schema:
            type: string
            format: uuid
        - name: paykey_id
          in: query
          description: Filter by the unique identifier of the paykey.
          schema:
            type: string
            format: uuid
        - name: paykey
          in: query
          description: Filter by the paykey token.
          schema:
            type: string
        - name: min_amount
          in: query
          description: Filter to payments with an amount in cents greater than or equal to
            this value.
          schema:
            type: integer
            format: int32
        - name: max_amount
          in: query
          description: Filter to payments with an amount in cents less than or equal to
            this value.
          schema:
            type: integer
            format: int32
        - name: min_payment_date
          in: query
          description: Filter to payments with a payment date on or after this date.
          schema:
            type: string
            format: date
        - name: max_payment_date
          in: query
          description: Filter to payments with a payment date on or before this date.
          schema:
            type: string
            format: date
        - name: min_created_at
          in: query
          description: Filter to payments created at or after this timestamp.
          schema:
            type: string
            format: date-time
        - name: max_created_at
          in: query
          description: Filter to payments created at or before this timestamp.
          schema:
            type: string
            format: date-time
        - name: min_effective_at
          in: query
          description: Filter to payments effective at or after this timestamp.
          schema:
            type: string
            format: date-time
        - name: max_effective_at
          in: query
          description: Filter to payments effective at or before this timestamp.
          schema:
            type: string
            format: date-time
        - name: funding_id
          in: query
          description: Filter by the unique identifier of a funding event.
          schema:
            type: string
            format: uuid
        - name: search_text
          in: query
          description: Free-text search across payment fields.
          schema:
            type: string
        - name: default_page_size
          in: query
          schema:
            type: integer
            format: int32
          description: Default number of results returned per page.
        - name: default_sort
          in: query
          schema:
            $ref: "#/components/schemas/PaymentSortBy"
          description: Default field used to sort the results.
        - name: default_sort_order
          in: query
          schema:
            $ref: "#/components/schemas/SortOrder"
          description: Default order in which to sort the results.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - name: status_reason
          in: query
          description: Filter by the reason for the most recent payment status change.
          schema:
            type: array
            items:
              $ref: "#/components/schemas/PaymentStatusReason"
        - name: status_source
          in: query
          description: Filter by the source of the most recent payment status change.
          schema:
            type: array
            items:
              $ref: "#/components/schemas/PaymentStatusSource"
        - name: include_metadata
          in: query
          description: Whether to include metadata in each returned payment. Defaults to
            false.
          schema:
            type: boolean
        - name: is_refund
          in: query
          description: Filter payouts by whether they refund an original charge.
          schema:
            type: boolean
        - name: has_refund
          in: query
          description: Filter charges by whether an associated payout has refunded them.
          schema:
            type: boolean
        - name: is_resubmit
          in: query
          description: Filter payments by whether they resubmit an original payment.
          schema:
            type: boolean
        - name: has_resubmit
          in: query
          description: Filter payments by whether they have been resubmitted.
          schema:
            type: boolean
        - name: min_updated_at
          in: query
          description: Filter to payments last updated on or after this timestamp.
          schema:
            type: string
            format: date-time
        - name: max_updated_at
          in: query
          description: Filter to payments last updated on or before this timestamp.
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PaymentSummaryListResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentSummaryListResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PaymentSummaryListResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/payouts/{id}:
    get:
      tags:
        - payouts
      summary: Get a payout
      description: Returns a payout by its unique identifier.
      operationId: getPayout
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the payout.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      tags:
        - payouts
      summary: Update a payout
      description: Updates the description, amount, `payment_date`, or metadata. The
        payout must have a status of `created` or `on_hold`.
      operationId: updatePayout
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the payout.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePayoutRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/UpdatePayoutRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/UpdatePayoutRequest"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/payouts:
    post:
      tags:
        - payouts
      summary: Create a payout
      description: Creates a payout to a customer's bank account. Straddle submits the
        payout for processing on `payment_date` unless the payout is on hold.
      operationId: createPayout
      parameters:
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePayoutRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/CreatePayoutRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/CreatePayoutRequest"
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/payouts/{id}/hold:
    put:
      tags:
        - payouts
      summary: Hold a payout
      description: Places a payout on hold to prevent submission for processing. The
        payout must have a status of `created` or `scheduled`.
      operationId: holdPayout
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the payout.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePayoutStatusRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/UpdatePayoutStatusRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/UpdatePayoutStatusRequest"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/payouts/{id}/release:
    put:
      tags:
        - payouts
      summary: Release a payout
      description: Releases a payout from `on_hold` and returns it to `created` for
        submission on `payment_date`.
      operationId: releasePayout
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the payout.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePayoutStatusRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/UpdatePayoutStatusRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/UpdatePayoutStatusRequest"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/payouts/{id}/cancel:
    put:
      tags:
        - payouts
      summary: Cancel a payout
      description: Cancels a payout. The payout must have a status of `created`,
        `scheduled`, or `on_hold`.
      operationId: cancelPayout
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the payout.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePayoutStatusRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/UpdatePayoutStatusRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/UpdatePayoutStatusRequest"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/organizations/{organization_id}:
    get:
      tags:
        - organizations
      summary: Get an organization
      description: Returns the organization with the specified ID.
      operationId: getOrganization
      parameters:
        - name: organization_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: The ID of the organization.
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/OrganizationResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/OrganizationResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
  /v1/representatives/{representative_id}/unmask:
    get:
      tags:
        - representatives
      summary: Get an unmasked representative
      description: Returns the representative with the specified ID without masking
        sensitive fields. This endpoint requires an administrator role.
      parameters:
        - name: representative_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: The ID of the representative.
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/UnmaskedRepresentativeResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/UnmaskedRepresentativeResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/UnmaskedRepresentativeResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
      operationId: getUnmaskedRepresentative
  /v1/paykeys/{id}/reveal:
    get:
      tags:
        - paykeys
      summary: Reveal a paykey token
      description: Returns a paykey by `id`, including the full paykey value and
        masked bank account details.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the paykey.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/RevealedPaykeyResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/RevealedPaykeyResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/RevealedPaykeyResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      operationId: revealPaykey
  /v1/customers/{id}/refresh_review:
    put:
      tags:
        - customers
      summary: Refresh a customer review
      description: Starts a new identity review for a customer. The review runs
        asynchronously. Webhooks and the customer review endpoint return updated
        results.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the customer.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "202":
          description: Accepted
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/CustomerResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/CustomerResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      operationId: refreshCustomerReview
  /v1/charges/{id}/unmask:
    get:
      tags:
        - charges
      summary: Get an unmasked charge
      description: Return a charge with its sensitive fields unmasked.
      operationId: getUnmaskedCharge
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the charge.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/UnmaskedChargeResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/UnmaskedChargeResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/UnmaskedChargeResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/payouts/{id}/unmask:
    get:
      tags:
        - payouts
      summary: Get an unmasked payout
      description: Return a payout with its sensitive fields unmasked.
      operationId: getUnmaskedPayout
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the payout.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/UnmaskedPayoutResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/UnmaskedPayoutResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/UnmaskedPayoutResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/paykeys/{id}/cancel:
    put:
      tags:
        - paykeys
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the paykey.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CancelPaykeyRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/CancelPaykeyRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/CancelPaykeyRequest"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      operationId: cancelPaykey
      summary: Cancel a paykey
      description: Cancels a paykey so it cannot be used for new payments.
  /v1/bridge/quiltt:
    post:
      tags:
        - bridge
      summary: Create a paykey from a Quiltt token
      description: Creates a paykey from a Quiltt processor token.
      parameters:
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateQuilttPaykeyRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/CreateQuilttPaykeyRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/CreateQuilttPaykeyRequest"
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/RevealedPaykeyResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/RevealedPaykeyResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/RevealedPaykeyResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      operationId: createQuilttPaykey
  /v1/paykeys/{id}/review:
    patch:
      tags:
        - paykeys
      summary: Set a paykey verification decision
      description: Updates the verification decision for a paykey. The paykey's
        current `status` must be `review`.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the paykey.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetPaykeyVerificationDecisionRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/SetPaykeyVerificationDecisionRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/SetPaykeyVerificationDecisionRequest"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      operationId: setPaykeyVerificationDecision
    get:
      tags:
        - paykeys
      summary: Get a paykey review
      description: Returns a paykey verification review, including the decision, score
        breakdowns, and result codes.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the paykey.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PaykeyReviewResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PaykeyReviewResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PaykeyReviewResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      operationId: getPaykeyReview
  /v1/linked_bank_accounts/{linked_bank_account_id}/cancel:
    patch:
      tags:
        - linkedBankAccounts
      summary: Cancel a linked bank account
      description: Cancels a linked bank account and returns it with status
        `canceled`. The linked bank account must have status `created`.
      parameters:
        - name: linked_bank_account_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: The ID of the linked bank account.
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/LinkedBankAccountResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/LinkedBankAccountResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/LinkedBankAccountResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
      operationId: cancelLinkedBankAccount
  /v1/paykeys/{id}/refresh_review:
    put:
      tags:
        - paykeys
      summary: Refresh a paykey review
      description: Starts a new verification review for a paykey. The review runs
        asynchronously. Webhooks and the paykey review endpoint return updated
        results.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the paykey.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "202":
          description: Accepted
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      operationId: refreshPaykeyReview
  /v1/paykeys/{id}/refresh_balance:
    put:
      tags:
        - paykeys
      summary: Refresh a paykey balance
      description: Starts an asynchronous balance refresh for a paykey. The response
        returns the paykey before the refresh finishes.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the paykey.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "202":
          description: Accepted
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      operationId: refreshPaykeyBalance
  /v1/funding_events/simulate:
    post:
      tags:
        - fundingEvents
      summary: Simulate a funding event
      description: Creates a funding event for unfunded charge or payout activity in
        the sandbox and returns its ID. This endpoint is unavailable in
        production.
      parameters:
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        description: Select the activity type and optional processing outcome for the
          simulated funding event.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SimulateFundingEventRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/SimulateFundingEventRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/SimulateFundingEventRequest"
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/FundingEventSimulationResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/FundingEventSimulationResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/FundingEventSimulationResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      operationId: simulateFundingEvent
  /v1/paykeys/{id}/unblock:
    patch:
      tags:
        - paykeys
      summary: Unblock a paykey
      description: Unblocks a paykey that was blocked by an `R29` return. The paykey
        must not have been unblocked before.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the paykey.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnblockPaykeyRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/UnblockPaykeyRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/UnblockPaykeyRequest"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PaykeyResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      operationId: unblockPaykey
  /v1/account_settings/{account_id}:
    get:
      tags:
        - accountSettings
      summary: Get account settings
      description: Returns all effective settings for the account, including values
        inherited from its organization, platform, and system defaults.
      operationId: getAccountSettings
      parameters:
        - name: account_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: The ID of the account.
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/AccountSettingsResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/AccountSettingsResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/AccountSettingsResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/EmbedErrorResponse"
  /v1/charges/{id}/resubmit:
    post:
      tags:
        - charges
      summary: Resubmit a charge
      description: Creates a new charge from a failed, reversed, or cancelled charge.
        The request can override `description`, `external_id`, and
        `payment_date`. Other payment details come from the original charge.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the charge.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResubmitChargeRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/ResubmitChargeRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/ResubmitChargeRequest"
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      operationId: resubmitCharge
  /v1/charges/{id}/refund:
    post:
      tags:
        - charges
      summary: Refund a paid charge
      description: Creates a payout to return funds from a paid charge to the
        customer's bank account. The payout is linked to the charge through
        `related_payments`. A charge can be refunded once, either fully or
        partially.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the charge.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefundChargeRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/RefundChargeRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/RefundChargeRequest"
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      operationId: refundCharge
  /v1/funding_event_payments/{id}:
    get:
      tags:
        - fundingEvents
      summary: List funding event payments
      description: Returns a paginated list of payments included in the funding event.
      operationId: listFundingEventPayments
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the funding event.
        - name: page_number
          in: query
          description: Results page number. Starts at 1. Defaults to 1.
          schema:
            type: integer
            format: int32
        - name: page_size
          in: query
          description: Number of results per page. Maximum 1,000. Defaults to 100.
          schema:
            type: integer
            format: int32
        - name: include_metadata
          in: query
          description: When `true`, includes each payment's metadata. Defaults to `false`.
          schema:
            type: boolean
        - name: default_page_size
          in: query
          schema:
            type: integer
            format: int32
          description: Default number of results returned per page.
        - name: default_sort
          in: query
          schema:
            $ref: "#/components/schemas/FundingEventPaymentSortBy"
          description: Default field used to sort the results.
        - name: default_sort_order
          in: query
          schema:
            $ref: "#/components/schemas/SortOrder"
          description: Default order in which to sort the results.
        - name: sort_by
          in: query
          schema:
            $ref: "#/components/schemas/FundingEventPaymentSortBy"
          description: Field used to sort the results.
        - name: sort_order
          in: query
          schema:
            $ref: "#/components/schemas/SortOrder"
          description: Order in which to sort the results.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/FundingEventPaymentListResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/FundingEventPaymentListResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/FundingEventPaymentListResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not Found
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/payouts/{id}/resubmit:
    post:
      tags:
        - payouts
      summary: Resubmit a payout
      description: Creates a new payout from a failed, reversed, or cancelled payout.
        The request can override `description`, `external_id`, and
        `payment_date`. Other payment details come from the original payout.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the payout.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResubmitPayoutRequest"
          text/json:
            schema:
              $ref: "#/components/schemas/ResubmitPayoutRequest"
          application/*+json:
            schema:
              $ref: "#/components/schemas/ResubmitPayoutRequest"
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      operationId: resubmitPayout
  /v1/charges/{id}/authorization:
    post:
      tags:
        - charges
      summary: Upload a proof-of-authorization document for a charge
      description: Uploads a proof-of-authorization document for a charge. A later
        upload adds another document and does not replace an existing one.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the charge.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          multipart/form-data:
            schema:
              required:
                - File
              type: object
              properties:
                File:
                  type: string
                  description: The document file to upload as proof of authorization for this
                    charge.
                  format: binary
            encoding:
              File:
                style: form
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ChargeResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      operationId: uploadChargeAuthorizationProof
  /v1/payouts/{id}/authorization:
    post:
      tags:
        - payouts
      summary: Upload a proof-of-authorization document for a payout
      description: Uploads a proof-of-authorization document for a payout. A later
        upload adds another document and does not replace an existing one.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier for the payout.
        - $ref: "#/components/parameters/StraddleAccountId"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/CorrelationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          multipart/form-data:
            schema:
              required:
                - File
              type: object
              properties:
                File:
                  type: string
                  description: The document file to upload as proof of authorization for this
                    payout.
                  format: binary
            encoding:
              File:
                style: form
      responses:
        "201":
          description: Created
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/PayoutResponse"
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Validation Failed
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server Error
          content:
            text/plain:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
            text/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      operationId: uploadPayoutAuthorizationProof
components:
  schemas:
    Account:
      required:
        - organization_id
        - status
        - id
        - type
        - status_detail
        - access_level
      type: object
      properties:
        id:
          type: string
          description: Straddle's unique ID for the account.
          format: uuid
        organization_id:
          type: string
          description: ID of the organization that owns the account.
          format: uuid
        type:
          enum:
            - business
          type: string
          description: The account type. Only `business` is supported.
        status:
          enum:
            - created
            - onboarding
            - active
            - rejected
            - inactive
          type: string
          description: The current lifecycle status of the account.
        status_detail:
          $ref: "#/components/schemas/AccountStatusDetail"
        business_profile:
          $ref: "#/components/schemas/AccountBusinessProfile"
        capabilities:
          $ref: "#/components/schemas/AccountCapabilities"
        settings:
          $ref: "#/components/schemas/AccountPaymentSettings"
        terms_of_service:
          $ref: "#/components/schemas/TermsOfService"
        metadata:
          type: object
          additionalProperties:
            type: string
            nullable: true
          description: Up to 20 user-defined key-value pairs.
          nullable: true
        access_level:
          enum:
            - standard
            - managed
          type: string
          description: The account access level. `standard` provides normal account
            access, including access to the Straddle dashboard. `managed` means
            the platform manages the account and account users cannot access the
            Straddle dashboard.
        external_id:
          type: string
          description: Your unique ID for the account.
          nullable: true
        created_at:
          type: string
          description: Date and time when Straddle created the account.
          format: date-time
          nullable: true
        updated_at:
          type: string
          description: Date and time of the most recent account update.
          format: date-time
          nullable: true
      additionalProperties: false
    AccountAddress:
      type: object
      nullable: true
      description: Optional business address. If provided, `line1`, `city`, `state`,
        and `postal_code` are required.
      properties:
        line1:
          type: string
          description: Primary address line, such as a street address or PO Box.
          nullable: true
        line2:
          type: string
          description: Secondary address line, such as an apartment, suite, unit, or
            building.
          nullable: true
        city:
          type: string
          description: City, district, suburb, town, or village.
          nullable: true
        state:
          type: string
          description: Two-letter state code.
          pattern: ^[A-Z]{2}$
          nullable: true
        postal_code:
          type: string
          description: Postal or ZIP code.
          pattern: ^[0-9]{5}(-[0-9]{4})?$
          nullable: true
        country:
          type: string
          description: Two-letter ISO 3166-1 country code. If omitted, Straddle applies US
            address validation.
          nullable: true
      additionalProperties: false
      required:
        - city
        - state
        - postal_code
        - line1
    UnmaskedLinkedBankAccountDetails:
      required:
        - institution_name
        - account_holder
        - routing_number
        - account_number
      type: object
      properties:
        institution_name:
          type: string
          description: Name of the financial institution.
        account_holder:
          type: string
          description: Name of the account holder as it appears on the bank account.
        routing_number:
          type: string
          description: Nine-digit ABA routing number for the bank account.
        account_number:
          type: string
          description: Bank account number.
      additionalProperties: false
    MaskedLinkedBankAccountDetails:
      required:
        - institution_name
        - account_holder
        - routing_number
        - account_mask
      type: object
      properties:
        institution_name:
          type: string
          description: Name of the financial institution.
        account_holder:
          type: string
          description: Name of the account holder as it appears on the bank account.
        routing_number:
          type: string
          description: Nine-digit ABA routing number for the bank account.
        account_mask:
          type: string
          description: Last four digits of the bank account number.
      additionalProperties: false
    LinkedBankAccountDetailsRequest:
      required:
        - account_holder
        - routing_number
        - account_number
      type: object
      properties:
        account_holder:
          type: string
          description: Account holder name as it appears on the bank account. This is
            usually the business's legal name.
        routing_number:
          type: string
          description: Nine-digit ABA routing number.
          minLength: 9
          maxLength: 9
        account_number:
          type: string
          description: The bank account number.
      additionalProperties: false
    AccountBusinessProfile:
      required:
        - website
        - name
      type: object
      properties:
        name:
          type: string
          description: The operating or trade name of the business.
        website:
          type: string
          format: uri
          description: URL of the business's primary website.
        legal_name:
          type: string
          description: The official registered name of the business.
          nullable: true
        description:
          type: string
          description: Description of the business and its products or services.
          nullable: true
        use_case:
          type: string
          description: How the business plans to use Straddle.
          nullable: true
        tax_id:
          type: string
          description: Business tax identification number, such as a US Employer
            Identification Number (EIN).
          pattern: ^[0-9]{9}$
          nullable: true
        phone:
          type: string
          description: Primary business phone number in E.164 format.
          pattern: ^\+[1-9]\d{1,14}$
          nullable: true
        address:
          $ref: "#/components/schemas/AccountAddress"
        industry:
          $ref: "#/components/schemas/AccountIndustry"
        support_channels:
          $ref: "#/components/schemas/AccountSupportChannels"
      additionalProperties: false
    CapabilitySetting:
      required:
        - enable
      type: object
      properties:
        enable:
          type: boolean
          description: Whether the request enables or disables the capability.
      additionalProperties: false
    AccountCapabilities:
      required:
        - customer_types
        - consent_types
        - payment_types
      type: object
      properties:
        payment_types:
          $ref: "#/components/schemas/AccountPaymentCapabilities"
        customer_types:
          $ref: "#/components/schemas/AccountCustomerCapabilities"
        consent_types:
          $ref: "#/components/schemas/AccountConsentCapabilities"
      additionalProperties: false
    AccountCapability:
      required:
        - capability_status
      type: object
      properties:
        capability_status:
          enum:
            - active
            - inactive
          type: string
          description: Status of the capability for the account.
      additionalProperties: false
    CapabilityRequest:
      required:
        - created_at
        - account_id
        - category
        - updated_at
        - status
        - id
        - type
        - enable
      type: object
      properties:
        id:
          type: string
          description: Straddle's unique ID for the capability request.
          format: uuid
        account_id:
          type: string
          description: ID of the account associated with the capability request.
          format: uuid
        type:
          enum:
            - charges
            - payouts
            - individuals
            - businesses
            - signed_agreement
            - internet
          type: string
          description: Capability type requested within the category.
        category:
          enum:
            - payment_type
            - customer_type
            - consent_type
          type: string
          description: Groups the requested capability. `payment_type` covers `charges`
            and `payouts`. `customer_type` covers `individuals` and
            `businesses`. `consent_type` covers `signed_agreement` and
            `internet` authorization.
        settings:
          type: object
          additionalProperties: {}
          description: Limits and other settings requested for the capability.
          nullable: true
        status:
          enum:
            - active
            - inactive
            - in_review
            - rejected
            - approved
            - reviewing
          type: string
          description: Status of the capability request.
        created_at:
          type: string
          description: Date and time when Straddle created the capability request.
          format: date-time
        updated_at:
          type: string
          description: Date and time of the most recent capability request update.
          format: date-time
        enable:
          type: boolean
          description: Whether the request enables or disables the capability.
      additionalProperties: false
    AccountChargeSettings:
      required:
        - max_amount
        - monthly_amount
        - monthly_count
        - daily_amount
        - linked_bank_account_id
        - funding_time
      type: object
      properties:
        max_amount:
          type: integer
          format: int32
          description: Maximum amount in cents for one charge.
        monthly_amount:
          type: integer
          format: int32
          description: Monthly charge amount limit in cents.
        daily_amount:
          type: integer
          format: int32
          description: Daily charge amount limit in cents.
        monthly_count:
          type: integer
          format: int32
          description: Maximum number of charges per calendar month.
        funding_time:
          enum:
            - immediate
            - next_day
            - one_day
            - two_day
            - three_day
            - four_day
            - five_day
          type: string
          description: Funding schedule for charges. Straddle sets this value.
        linked_bank_account_id:
          type: string
          format: uuid
          description: ID of the linked bank account used for charge settlement. Straddle
            sets this value.
      additionalProperties: false
    ChargeCapabilityRequest:
      required:
        - max_amount
        - monthly_amount
        - monthly_count
        - daily_amount
        - enable
      type: object
      properties:
        enable:
          type: boolean
          description: Whether to enable or disable charges for the account.
        max_amount:
          type: number
          format: double
          description: Maximum amount in cents for one charge.
        daily_amount:
          type: number
          format: double
          description: Daily charge amount limit in cents.
        monthly_count:
          type: integer
          format: int32
          description: Maximum number of charges per calendar month.
        monthly_amount:
          type: number
          format: double
          description: Monthly charge amount limit in cents.
      additionalProperties: false
    AccountConsentCapabilities:
      required:
        - internet
        - signed_agreement
      type: object
      properties:
        signed_agreement:
          $ref: "#/components/schemas/AccountCapability"
          description: Signed-agreement payment authorization capability for the account.
        internet:
          $ref: "#/components/schemas/AccountCapability"
          description: Internet payment authorization capability for the account.
      additionalProperties: false
    CreateAccountRequest:
      required:
        - business_profile
        - access_level
        - account_type
        - organization_id
      type: object
      properties:
        organization_id:
          type: string
          description: ID of the organization that will own the account.
          format: uuid
        account_type:
          enum:
            - business
          type: string
          description: Account type. The only accepted value is `business`.
        business_profile:
          $ref: "#/components/schemas/AccountBusinessProfile"
        access_level:
          enum:
            - standard
            - managed
          type: string
          description: The account access level. `standard` provides normal account
            access, including access to the Straddle dashboard. `managed` means
            the platform manages the account and account users cannot access the
            Straddle dashboard.
        metadata:
          type: object
          additionalProperties:
            type: string
            nullable: true
          description: Up to 20 user-defined key-value pairs.
          nullable: true
        external_id:
          type: string
          description: Your unique ID for the account.
          nullable: true
      additionalProperties: false
    CreateOrganizationRequest:
      required:
        - name
      type: object
      properties:
        name:
          type: string
          description: Organization name.
        metadata:
          type: object
          additionalProperties:
            type: string
            nullable: true
          description: Up to 20 user-defined key-value pairs.
          nullable: true
        external_id:
          type: string
          description: Your unique ID for the organization.
          nullable: true
      additionalProperties: false
    CreateRepresentativeRequest:
      required:
        - dob
        - account_id
        - first_name
        - ssn_last4
        - last_name
        - email
        - relationship
        - mobile_number
      type: object
      properties:
        account_id:
          type: string
          description: ID of the account associated with the representative.
          format: uuid
        first_name:
          type: string
          description: Representative's first name.
        last_name:
          type: string
          description: Representative's last name.
        dob:
          type: string
          description: Representative's date of birth in `YYYY-MM-DD` format.
          format: date
          example: 1980-01-01
        ssn_last4:
          type: string
          description: Last four digits of the representative's Social Security number.
          pattern: ^[0-9]{4}$
          example: "1234"
        email:
          type: string
          description: Representative's company email address.
          format: email
          example: ron.swanson@pawnee.com
        mobile_number:
          type: string
          description: Representative's mobile phone number in E.164 format.
          pattern: ^\+[1-9]\d{1,14}$
          example: "+12128675309"
        relationship:
          $ref: "#/components/schemas/RepresentativeRelationship"
        external_id:
          type: string
          description: Your unique ID for the representative.
          nullable: true
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Up to 20 user-defined key-value pairs.
          nullable: true
      additionalProperties: false
    AccountCustomerCapabilities:
      required:
        - businesses
        - individuals
      type: object
      properties:
        individuals:
          $ref: "#/components/schemas/AccountCapability"
        businesses:
          $ref: "#/components/schemas/AccountCapability"
      additionalProperties: false
    EmbedError:
      required:
        - detail
        - status
        - items
        - type
        - title
      type: object
      properties:
        status:
          type: integer
          format: int32
          description: HTTP status code associated with the error.
        type:
          type: string
          description: Identifier for the error type.
        title:
          type: string
          description: Short summary of the error.
        detail:
          type: string
          description: Explanation of the error.
        items:
          type: array
          items:
            $ref: "#/components/schemas/EmbedErrorDetail"
          description: Additional error details.
      additionalProperties: false
    EmbedErrorDetail:
      required:
        - detail
        - reference
      type: object
      properties:
        reference:
          type: string
          description: Request field or element associated with this detail.
        detail:
          type: string
          description: Explanation of this error detail.
      additionalProperties: false
    EmbedErrorResponse:
      required:
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/EmbedResponseMetadata"
        response_type:
          enum:
            - object
            - array
            - error
          type: string
          description: |-
            Shape of the response envelope.
            - `object` means `data` contains one JSON object.
            - `array` means `data` contains an array of JSON objects.
            - `error` means `error` contains the error details.
        data:
          nullable: true
          description: Response data, when available.
        error:
          $ref: "#/components/schemas/EmbedError"
      additionalProperties: false
    AccountIndustry:
      type: object
      properties:
        mcc:
          type: string
          description: Merchant category code (MCC) that best describes the business. If
            omitted, provide both `sector` and `category`.
          nullable: true
        sector:
          type: string
          description: Business sector. Required when `mcc` is omitted.
          nullable: true
        category:
          type: string
          description: Industry category. Required when `mcc` is omitted.
          nullable: true
      additionalProperties: false
    AccountResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          enum:
            - object
            - array
            - error
            - none
          type: string
          description: |-
            Indicates how the response content is structured.
            - `object` means `data` contains one JSON object.
            - `array` means `data` contains an array of objects.
            - `error` means `error` contains error details.
            - `none` means the response has no data.
        data:
          $ref: "#/components/schemas/Account"
      additionalProperties: false
    UnmaskedLinkedBankAccountResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          enum:
            - object
            - array
            - error
            - none
          type: string
          description: |-
            Indicates how the response content is structured.
            - `object` means `data` contains one JSON object.
            - `array` means `data` contains an array of objects.
            - `error` means `error` contains error details.
            - `none` means the response has no data.
        data:
          $ref: "#/components/schemas/UnmaskedLinkedBankAccount"
      additionalProperties: false
    LinkedBankAccountResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          enum:
            - object
            - array
            - error
            - none
          type: string
          description: |-
            Indicates how the response content is structured.
            - `object` means `data` contains one JSON object.
            - `array` means `data` contains an array of objects.
            - `error` means `error` contains error details.
            - `none` means the response has no data.
        data:
          $ref: "#/components/schemas/LinkedBankAccount"
      additionalProperties: false
    OrganizationResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          enum:
            - object
            - array
            - error
            - none
          type: string
          description: |-
            Indicates how the response content is structured.
            - `object` means `data` contains one JSON object.
            - `array` means `data` contains an array of objects.
            - `error` means `error` contains error details.
            - `none` means the response has no data.
        data:
          $ref: "#/components/schemas/Organization"
      additionalProperties: false
    RepresentativeResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          enum:
            - object
            - array
            - error
            - none
          type: string
          description: |-
            Indicates how the response content is structured.
            - `object` means `data` contains one JSON object.
            - `array` means `data` contains an array of objects.
            - `error` means `error` contains error details.
            - `none` means the response has no data.
        data:
          $ref: "#/components/schemas/Representative"
      additionalProperties: false
    UnmaskedLinkedBankAccount:
      required:
        - created_at
        - account_id
        - status
        - updated_at
        - id
        - bank_account
        - status_detail
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Straddle's unique ID for the linked bank account.
        account_id:
          type: string
          format: uuid
          description: ID of the Straddle account associated with the linked bank account.
        status:
          enum:
            - created
            - onboarding
            - active
            - rejected
            - inactive
            - canceled
          type: string
          description: Status of the linked bank account.
        status_detail:
          $ref: "#/components/schemas/LinkedBankAccountStatusDetail"
          description: Details about the linked bank account's status.
        bank_account:
          $ref: "#/components/schemas/UnmaskedLinkedBankAccountDetails"
          description: Unmasked bank account details.
        metadata:
          type: object
          additionalProperties:
            type: string
            nullable: true
          nullable: true
        created_at:
          type: string
          format: date-time
          description: Date and time when Straddle created the linked bank account.
        updated_at:
          type: string
          format: date-time
          description: Date and time of the most recent linked bank account update.
      additionalProperties: false
    LinkedBankAccount:
      required:
        - created_at
        - account_id
        - status
        - updated_at
        - id
        - bank_account
        - status_detail
        - purposes
      type: object
      properties:
        id:
          type: string
          description: Straddle's unique ID for the linked bank account.
          format: uuid
        account_id:
          type: string
          description: ID of the related account, if this is an account-level linked bank
            account.
          format: uuid
          nullable: true
        status:
          enum:
            - created
            - onboarding
            - active
            - rejected
            - inactive
            - canceled
          type: string
          description: Status of the linked bank account.
        status_detail:
          $ref: "#/components/schemas/LinkedBankAccountStatusDetail"
        bank_account:
          $ref: "#/components/schemas/MaskedLinkedBankAccountDetails"
        metadata:
          type: object
          additionalProperties:
            type: string
            nullable: true
          description: Up to 20 user-defined key-value pairs.
          nullable: true
        created_at:
          type: string
          description: Date and time when Straddle created the linked bank account.
          format: date-time
        updated_at:
          type: string
          description: Date and time of the most recent linked bank account update.
          format: date-time
        platform_id:
          type: string
          description: ID of the related platform, if this is a platform-level linked bank
            account.
          format: uuid
          nullable: true
        purposes:
          type: array
          items:
            enum:
              - charges
              - payouts
              - billing
            type: string
          description: Payment purposes assigned to the linked bank account.
        description:
          type: string
          description: Your description for the linked bank account.
          nullable: true
      additionalProperties: false
    EmbedResponseMetadata:
      required:
        - api_request_timestamp
        - api_request_id
        - sort_by
        - sort_order
      type: object
      description: Metadata for an API request and an optional page of results.
      properties:
        api_request_id:
          type: string
          description: Unique identifier for the API request.
        api_request_timestamp:
          type: string
          description: Timestamp for the API request.
        page_number:
          type: integer
          format: int32
          nullable: true
          description: Current page number.
        page_size:
          type: integer
          format: int32
          nullable: true
          description: Number of items per page.
        total_rows:
          type: integer
          format: int32
          nullable: true
          description: Total number of items available across all pages.
        sort_order:
          type: string
          description: Sort direction for the results.
        sort_by:
          type: string
          description: Field used to sort the results.
      additionalProperties: false
    OnboardAccountRequest:
      required:
        - terms_of_service
      type: object
      description: Terms of Service acceptance details for the account.
      properties:
        terms_of_service:
          $ref: "#/components/schemas/TermsOfService"
      additionalProperties: false
    Organization:
      required:
        - id
        - updated_at
        - name
        - created_at
      type: object
      properties:
        id:
          type: string
          description: Straddle's unique ID for the organization.
          format: uuid
        name:
          type: string
          description: The name of the organization.
        external_id:
          type: string
          description: Your unique ID for the organization.
          nullable: true
        metadata:
          type: object
          additionalProperties:
            type: string
            nullable: true
          description: Up to 20 user-defined key-value pairs.
          nullable: true
        created_at:
          type: string
          description: Date and time when Straddle created the organization.
          format: date-time
        updated_at:
          type: string
          description: Date and time of the most recent organization update.
          format: date-time
      additionalProperties: false
    PageMetadata:
      required:
        - page_number
        - sort_by
        - sort_order
        - total_items
        - api_request_id
        - api_request_timestamp
        - max_page_size
        - total_pages
        - page_size
      type: object
      description: Metadata for an API request and a page of results.
      properties:
        api_request_id:
          type: string
          description: Unique identifier for the API request.
          format: uuid
        api_request_timestamp:
          type: string
          description: UTC timestamp for the API request.
          format: date-time
        total_items:
          type: integer
          format: int32
          description: Total number of items available across all pages.
        page_number:
          type: integer
          description: Current page number.
          format: int32
        page_size:
          type: integer
          description: Number of items per page.
          format: int32
        max_page_size:
          type: integer
          description: Maximum page size allowed for this endpoint.
          format: int32
        sort_by:
          type: string
          description: Field used to sort the results.
        sort_order:
          $ref: "#/components/schemas/SortOrder"
        total_pages:
          type: integer
          description: Total number of pages available.
          format: int32
      additionalProperties: false
    AccountListResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/PageMetadata"
        response_type:
          enum:
            - object
            - array
            - error
            - none
          type: string
          description: |-
            Indicates how the response content is structured.
            - `object` means `data` contains one JSON object.
            - `array` means `data` contains an array of objects.
            - `error` means `error` contains error details.
            - `none` means the response has no data.
        data:
          type: array
          items:
            $ref: "#/components/schemas/Account"
          description: Accounts returned for this page.
      additionalProperties: false
    CapabilityRequestListResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/PageMetadata"
        response_type:
          enum:
            - object
            - array
            - error
            - none
          type: string
          description: |-
            Indicates how the response content is structured.
            - `object` means `data` contains one JSON object.
            - `array` means `data` contains an array of objects.
            - `error` means `error` contains error details.
            - `none` means the response has no data.
        data:
          type: array
          items:
            $ref: "#/components/schemas/CapabilityRequest"
          description: Capability requests returned for this page.
      additionalProperties: false
    LinkedBankAccountListResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/PageMetadata"
        response_type:
          enum:
            - object
            - array
            - error
            - none
          type: string
          description: |-
            Indicates how the response content is structured.
            - `object` means `data` contains one JSON object.
            - `array` means `data` contains an array of objects.
            - `error` means `error` contains error details.
            - `none` means the response has no data.
        data:
          type: array
          items:
            $ref: "#/components/schemas/LinkedBankAccount"
          description: Linked bank accounts returned for this page.
      additionalProperties: false
    OrganizationListResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/PageMetadata"
        response_type:
          enum:
            - object
            - array
            - error
            - none
          type: string
          description: |-
            Indicates how the response content is structured.
            - `object` means `data` contains one JSON object.
            - `array` means `data` contains an array of objects.
            - `error` means `error` contains error details.
            - `none` means the response has no data.
        data:
          type: array
          items:
            $ref: "#/components/schemas/Organization"
          description: Organizations returned for this page.
      additionalProperties: false
    RepresentativeListResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/PageMetadata"
        response_type:
          enum:
            - object
            - array
            - error
            - none
          type: string
          description: |-
            Indicates how the response content is structured.
            - `object` means `data` contains one JSON object.
            - `array` means `data` contains an array of objects.
            - `error` means `error` contains error details.
            - `none` means the response has no data.
        data:
          type: array
          items:
            $ref: "#/components/schemas/Representative"
          description: Representatives returned for this page.
      additionalProperties: false
    AccountPaymentCapabilities:
      required:
        - charges
        - payouts
      type: object
      properties:
        charges:
          $ref: "#/components/schemas/AccountCapability"
        payouts:
          $ref: "#/components/schemas/AccountCapability"
      additionalProperties: false
    AccountPayoutSettings:
      required:
        - max_amount
        - monthly_amount
        - monthly_count
        - daily_amount
        - linked_bank_account_id
        - funding_time
      type: object
      properties:
        max_amount:
          type: integer
          format: int32
          description: Maximum amount in cents for one payout.
        monthly_amount:
          type: integer
          format: int32
          description: Monthly payout amount limit in cents.
        daily_amount:
          type: integer
          format: int32
          description: Daily payout amount limit in cents.
        monthly_count:
          type: integer
          format: int32
          description: Maximum number of payouts per calendar month.
        funding_time:
          enum:
            - immediate
            - next_day
            - one_day
            - two_day
            - three_day
            - four_day
            - five_day
          type: string
          description: Funding schedule for payouts. Straddle sets this value.
        linked_bank_account_id:
          type: string
          format: uuid
          description: ID of the linked bank account used for payout settlement.
      additionalProperties: false
    PayoutCapabilityRequest:
      required:
        - max_amount
        - monthly_amount
        - monthly_count
        - daily_amount
        - enable
      type: object
      properties:
        enable:
          type: boolean
          description: Whether to enable or disable payouts for the account.
        max_amount:
          type: number
          format: double
          description: Maximum amount in cents for one payout.
        daily_amount:
          type: number
          format: double
          description: Daily payout amount limit in cents.
        monthly_count:
          type: integer
          format: int32
          description: Maximum number of payouts per calendar month.
        monthly_amount:
          type: number
          format: double
          description: Monthly payout amount limit in cents.
      additionalProperties: false
    RepresentativeRelationship:
      required:
        - owner
        - control
        - primary
      type: object
      properties:
        primary:
          type: boolean
          description: Whether this person is the account's primary representative. The
            primary representative provides personal and business information
            and accepts the services agreement. An account can have only one
            primary representative.
        control:
          type: boolean
          description: Whether the representative controls, manages, or directs the
            business. Each legal entity must have one representative with
            `control` set to `true`.
        owner:
          type: boolean
          description: Whether the representative owns any equity in the business.
        percent_ownership:
          type: number
          description: The representative's ownership percentage. Required when `owner` is
            `true`.
          format: double
          nullable: true
        title:
          type: string
          description: The representative's job title.
          nullable: true
      additionalProperties: false
    Representative:
      required:
        - dob
        - created_at
        - account_id
        - first_name
        - status
        - updated_at
        - ssn_last4
        - last_name
        - id
        - email
        - relationship
        - status_detail
        - mobile_number
        - name
      type: object
      properties:
        id:
          type: string
          description: Straddle's unique ID for the representative.
          format: uuid
        account_id:
          type: string
          description: ID of the account associated with the representative.
          format: uuid
        user_id:
          type: string
          description: ID of the Straddle user linked to the representative, if any.
          format: uuid
          nullable: true
        status:
          enum:
            - created
            - onboarding
            - active
            - rejected
            - inactive
          type: string
          description: Status of the representative.
        status_detail:
          $ref: "#/components/schemas/RepresentativeStatusDetail"
        first_name:
          type: string
          description: Representative's first name.
          example: Ron
        last_name:
          type: string
          description: Representative's last name.
          example: Swanson
        dob:
          type: string
          description: Representative's date of birth in `YYYY-MM-DD` format.
          format: date
          example: 1980-01-01
        ssn_last4:
          type: string
          description: Last four digits of the representative's Social Security number.
          example: "1234"
        email:
          type: string
          description: Representative's email address.
          format: email
          example: ron.swanson@pawnee.com
          nullable: true
        mobile_number:
          type: string
          description: Representative's mobile phone number in E.164 format.
          pattern: ^\+[1-9]\d{1,14}$
          example: "+12128675309"
        relationship:
          $ref: "#/components/schemas/RepresentativeRelationship"
        external_id:
          type: string
          description: Your unique ID for the representative.
          nullable: true
        created_at:
          type: string
          description: Date and time when Straddle created the representative.
          format: date-time
        updated_at:
          type: string
          description: Date and time of the most recent representative update.
          format: date-time
        name:
          type: string
          description: Representative's display name.
        phone:
          type: string
          nullable: true
          description: Representative's phone number.
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Up to 20 user-defined key-value pairs.
          nullable: true
      additionalProperties: false
    RequestCapabilityRequest:
      type: object
      properties:
        charges:
          $ref: "#/components/schemas/ChargeCapabilityRequest"
          description: Requested charge capability and limits.
        payouts:
          $ref: "#/components/schemas/PayoutCapabilityRequest"
          description: Requested payout capability and limits.
        internet:
          $ref: "#/components/schemas/CapabilitySetting"
          description: Request to enable or disable internet and mobile payment
            authorization.
        individuals:
          $ref: "#/components/schemas/CapabilitySetting"
          description: Request to enable or disable payments from individuals.
        businesses:
          $ref: "#/components/schemas/CapabilitySetting"
          description: Request to enable or disable payments from businesses.
        signed_agreement:
          $ref: "#/components/schemas/CapabilitySetting"
          description: Request to enable or disable signed-agreement payment authorization.
      additionalProperties: false
    ResponseMetadata:
      required:
        - api_request_id
        - api_request_timestamp
      type: object
      description: Metadata for an API request.
      properties:
        api_request_id:
          type: string
          description: Unique identifier for the API request.
          format: uuid
        api_request_timestamp:
          type: string
          description: UTC timestamp for the API request.
          format: date-time
      additionalProperties: false
    AccountPaymentSettings:
      required:
        - charges
        - payouts
      type: object
      properties:
        charges:
          $ref: "#/components/schemas/AccountChargeSettings"
        payouts:
          $ref: "#/components/schemas/AccountPayoutSettings"
      additionalProperties: false
    AccountStatusDetail:
      required:
        - message
        - code
        - reason
        - source
      type: object
      properties:
        reason:
          enum:
            - unverified
            - in_review
            - pending
            - stuck
            - verified
            - failed_verification
            - disabled
            - terminated
            - new
          type: string
          description: Machine-readable reason for the account's status.
        source:
          enum:
            - watchtower
          type: string
          description: System that produced the account status detail.
        code:
          type: string
          description: Machine-readable status code from the source.
        message:
          type: string
          description: Human-readable description of the account's status.
      additionalProperties: false
    LinkedBankAccountStatusDetail:
      required:
        - message
        - code
        - reason
        - source
      type: object
      properties:
        reason:
          enum:
            - unverified
            - in_review
            - pending
            - stuck
            - verified
            - failed_verification
            - disabled
            - new
          type: string
          description: Machine-readable reason for the linked bank account's status.
        source:
          enum:
            - watchtower
          type: string
          description: System that produced the linked bank account status detail.
        code:
          type: string
          description: Machine-readable status code from the source.
        message:
          type: string
          description: Human-readable description of the linked bank account's status.
      additionalProperties: false
    RepresentativeStatusDetail:
      required:
        - message
        - code
        - reason
        - source
      type: object
      properties:
        reason:
          enum:
            - unverified
            - in_review
            - pending
            - stuck
            - verified
            - failed_verification
            - disabled
            - new
          type: string
          description: Machine-readable reason for the representative's status.
        source:
          enum:
            - watchtower
          type: string
          description: System that produced the representative status detail.
        code:
          type: string
          description: Machine-readable status code from the source.
        message:
          type: string
          description: Human-readable description of the representative's status.
      additionalProperties: false
    AccountSupportChannels:
      type: object
      properties:
        email:
          type: string
          description: Email address for customer support.
          format: email
          nullable: true
        phone:
          type: string
          description: Customer support phone number in E.164 format.
          pattern: ^\+[1-9]\d{1,14}$
          nullable: true
        url:
          type: string
          description: URL of the business's customer support page or contact form.
          format: uri
          nullable: true
      additionalProperties: false
    TermsOfService:
      required:
        - accepted_date
        - agreement_type
        - agreement_url
      type: object
      properties:
        accepted_date:
          type: string
          description: Date and time when the account accepted the Terms of Service.
          format: date-time
        accepted_ip:
          type: string
          description: IP address used to accept the Terms of Service.
          nullable: true
        accepted_user_agent:
          type: string
          description: User agent of the browser or application that accepted the Terms of
            Service.
          nullable: true
        agreement_url:
          type: string
          description: URL of the accepted agreement.
          nullable: true
        agreement_type:
          enum:
            - embedded
            - direct
          type: string
          description: Agreement type. Use `embedded` unless Straddle has enabled the
            platform for `direct` agreements.
          default: embedded
      additionalProperties: false
    UpdateAccountRequest:
      required:
        - business_profile
      type: object
      properties:
        business_profile:
          $ref: "#/components/schemas/AccountBusinessProfile"
        metadata:
          type: object
          additionalProperties:
            type: string
            nullable: true
          description: Up to 20 user-defined key-value pairs.
          nullable: true
        external_id:
          type: string
          description: Your unique ID for the account.
          nullable: true
      additionalProperties: false
    UpdateLinkedBankAccountRequest:
      required:
        - bank_account
      type: object
      properties:
        bank_account:
          $ref: "#/components/schemas/LinkedBankAccountDetailsRequest"
        metadata:
          type: object
          additionalProperties:
            type: string
            nullable: true
          description: Up to 20 user-defined key-value pairs.
          nullable: true
      additionalProperties: false
    UpdateRepresentativeRequest:
      required:
        - dob
        - first_name
        - ssn_last4
        - last_name
        - email
        - relationship
        - mobile_number
      type: object
      properties:
        first_name:
          type: string
          description: Representative's first name.
          example: Ron
        last_name:
          type: string
          description: Representative's last name.
          example: Swanson
        dob:
          type: string
          description: Representative's date of birth in `YYYY-MM-DD` format.
          format: date
          example: 1980-01-01
        ssn_last4:
          type: string
          description: Last four digits of the representative's Social Security number.
          pattern: ^[0-9]{4}$
          example: "1234"
        email:
          type: string
          description: Representative's email address.
          format: email
          example: ron.swanson@pawnee.com
        mobile_number:
          type: string
          description: Representative's mobile phone number in E.164 format.
          pattern: ^\+[1-9]\d{1,14}$
          example: "+12128675309"
        relationship:
          $ref: "#/components/schemas/RepresentativeRelationship"
        external_id:
          type: string
          description: Your unique ID for the representative.
          nullable: true
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Up to 20 user-defined key-value pairs.
          nullable: true
      additionalProperties: false
    AccountType:
      enum:
        - checking
        - savings
      type: string
    CustomerAddress:
      required:
        - state
        - address1
        - city
        - zip
      type: object
      description: Customer postal address. When provided, the object must include all
        required fields.
      nullable: true
      properties:
        address1:
          type: string
          description: Primary address line, such as a street address or PO Box.
          example: 123 Main St
          maxLength: 100
        address2:
          type: string
          description: Secondary address line, such as an apartment, suite, unit, or
            building.
          example: Apt 1
          maxLength: 100
          nullable: true
        city:
          type: string
          description: City, district, suburb, town, or village.
          example: Anytown
          maxLength: 100
        state:
          type: string
          description: Two-letter state code.
          example: CA
          pattern: ^[A-Z]{2}$
        zip:
          type: string
          description: ZIP or postal code.
          example: "12345"
          pattern: ^[0-9]{5}(-[0-9]{4})?$
      additionalProperties: false
    BridgeToken:
      required:
        - bridge_token
      type: object
      properties:
        bridge_token:
          type: string
          description: JSON Web Token (JWT) for the Bridge widget.
      additionalProperties: false
    BridgeTokenResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        data:
          $ref: "#/components/schemas/BridgeToken"
      additionalProperties: false
    UnmaskedComplianceProfile:
      oneOf:
        - type: object
          title: Individual Compliance Profile
          description: Personally identifiable information used for Patriot Act-compliant
            Know Your Customer (KYC) verification.
          properties:
            ssn:
              type: string
              pattern: ^[0-9]{3}-[0-9]{2}-[0-9]{4}$
              description: Social Security number in `XXX-XX-XXXX` format. Required for
                Patriot Act-compliant KYC verification.
              nullable: true
              example: 123-45-6789
            dob:
              type: string
              format: date
              description: Date of birth in `YYYY-MM-DD` format. Required for Patriot
                Act-compliant KYC verification.
              nullable: true
              example: 1969-04-20
          required:
            - ssn
            - dob
        - type: object
          title: Business Compliance Profile
          description: Business registration information used for Patriot Act-compliant
            Know Your Business (KYB) verification.
          properties:
            ein:
              type: string
              pattern: ^[0-9]{2}-[0-9]{7}$
              description: Employer Identification Number in `XX-XXXXXXX` format. Required for
                Patriot Act-compliant KYB verification.
              nullable: true
              example: 12-3456789
            legal_business_name:
              type: string
              description: Official business name registered with the IRS.
              nullable: true
              example: Acme Corp LLC
            website:
              type: string
              format: uri
              description: Official business website URL.
              nullable: true
              example: https://example.com
            representatives:
              type: array
              items:
                $ref: "#/components/schemas/BusinessCustomerRepresentative"
              description: Representatives associated with the business. Valid only for
                `business` customers.
              nullable: true
          required:
            - ein
            - legal_business_name
    ComplianceProfile:
      oneOf:
        - type: object
          title: Individual Compliance Profile
          description: Masked personally identifiable information used for Patriot
            Act-compliant KYC verification.
          properties:
            dob:
              type: string
              description: Masked date of birth in `****-**-**` format.
              nullable: true
              example: "****-**-**"
            ssn:
              type: string
              description: Masked Social Security number in `***-**-****` format.
              nullable: true
              example: "***-**-****"
          required:
            - dob
            - ssn
        - type: object
          title: Business Compliance Profile
          description: Masked business registration information used for Patriot
            Act-compliant KYB verification.
          properties:
            ein:
              type: string
              description: Masked Employer Identification Number in `**-*******` format.
              nullable: true
              example: "**-*******"
            legal_business_name:
              type: string
              description: Official registered business name associated with `ein`.
              nullable: true
              example: Acme Corp LLC
            website:
              type: string
              format: uri
              description: Official business website URL.
              nullable: true
              example: https://example.com
            representatives:
              type: array
              items:
                $ref: "#/components/schemas/BusinessCustomerRepresentative"
              description: Representatives associated with the business. Valid only for
                `business` customers.
              nullable: true
          required:
            - ein
            - legal_business_name
    CreateBankAccountPaykeyRequest:
      required:
        - account_type
        - routing_number
        - customer_id
        - account_number
      type: object
      properties:
        customer_id:
          type: string
          description: Unique identifier for the customer associated with the paykey.
          format: uuid
        routing_number:
          type: string
          description: Bank routing number.
          minLength: 9
          maxLength: 9
        account_number:
          type: string
          description: Bank account number.
        account_type:
          $ref: "#/components/schemas/AccountType"
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Up to 20 user-defined key-value pairs associated with the paykey.
          nullable: true
        config:
          $ref: "#/components/schemas/PaykeyConfiguration"
        external_id:
          type: string
          description: Unique identifier for the paykey in your system.
          nullable: true
      additionalProperties: false
    CreateBridgeTokenRequest:
      required:
        - customer_id
      type: object
      properties:
        customer_id:
          type: string
          description: Unique identifier for the customer associated with the Bridge
            session.
          format: uuid
        config:
          $ref: "#/components/schemas/PaykeyConfiguration"
        external_id:
          type: string
          description: Unique identifier for the paykey in your system.
          nullable: true
      additionalProperties: false
    CreateCustomerRequest:
      required:
        - name
        - phone
        - device
        - email
        - type
      type: object
      properties:
        name:
          type: string
          description: Full name for an individual customer or business name for a
            business customer.
          example: Ron Swanson
        type:
          $ref: "#/components/schemas/CustomerType"
        email:
          type: string
          description: Customer email address.
          format: email
          example: ron.swanson@pawnee.com
        address:
          $ref: "#/components/schemas/CustomerAddress"
          description: Customer postal address. When provided, the object must include all
            required fields.
        phone:
          type: string
          pattern: ^\+[1-9]\d{1,14}$
          description: Customer phone number in E.164 format. A mobile number is preferred.
          example: "+12128675309"
        compliance_profile:
          $ref: "#/components/schemas/UnmaskedComplianceProfile"
          description: Customer compliance profile. When provided, the object must include
            all fields required for the customer type.
          nullable: true
        external_id:
          type: string
          description: Unique identifier for the customer in your system.
          example: customer_123
          nullable: true
        device:
          $ref: "#/components/schemas/CustomerDevice"
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Up to 20 user-defined key-value pairs associated with the customer.
          nullable: true
        config:
          $ref: "#/components/schemas/CustomerConfiguration"
      additionalProperties: false
      example:
        name: Ron Swanson
        type: individual
        email: ron.swanson@pawnee.com
        address:
          address1: 123 Main St
          city: Anytown
          state: CA
          zip: "94105"
        phone: "+12128675309"
        external_id: customer_123
        device:
          ip_address: 192.168.1.1
        metadata: {}
    CreatePlaidPaykeyRequest:
      required:
        - plaid_token
        - customer_id
      type: object
      properties:
        customer_id:
          type: string
          description: Unique identifier for the customer associated with the paykey.
          format: uuid
        plaid_token:
          type: string
          description: Plaid processor token generated by your application for use with
            the Straddle API.
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Up to 20 user-defined key-value pairs associated with the paykey.
          nullable: true
        config:
          $ref: "#/components/schemas/PaykeyConfiguration"
        external_id:
          type: string
          description: Unique identifier for the paykey in your system.
          nullable: true
      additionalProperties: false
    CustomerIdentityVerification:
      required:
        - created_at
        - updated_at
        - decision
        - review_id
        - breakdown
      type: object
      properties:
        review_id:
          type: string
          description: Unique identifier for the review.
          format: uuid
        decision:
          $ref: "#/components/schemas/VerificationDecision"
        messages:
          type: object
          additionalProperties:
            type: string
          description: Messages returned by the customer verification process.
          nullable: true
        breakdown:
          type: object
          properties:
            address:
              $ref: "#/components/schemas/IdentityVerificationBreakdown"
            email:
              $ref: "#/components/schemas/IdentityVerificationBreakdown"
            fraud:
              $ref: "#/components/schemas/IdentityVerificationBreakdown"
            phone:
              $ref: "#/components/schemas/IdentityVerificationBreakdown"
            synthetic:
              $ref: "#/components/schemas/IdentityVerificationBreakdown"
            business_identification:
              $ref: "#/components/schemas/IdentityVerificationBreakdown"
            business_validation:
              $ref: "#/components/schemas/IdentityVerificationBreakdown"
            business_evaluation:
              $ref: "#/components/schemas/IdentityVerificationBreakdown"
          additionalProperties: false
          description: Results for each customer verification check, including decisions,
            risk scores, and correlation scores.
        network_alerts:
          $ref: "#/components/schemas/IdentityVerificationAlerts"
        watch_list:
          $ref: "#/components/schemas/IdentityVerificationWatchlist"
        kyc:
          $ref: "#/components/schemas/CustomerKycVerification"
        created_at:
          type: string
          description: Timestamp of when the review was initiated.
          format: date-time
        updated_at:
          type: string
          description: Timestamp of the most recent update to the review.
          format: date-time
        reputation:
          $ref: "#/components/schemas/ReputationCheck"
      additionalProperties: false
    CustomerReview:
      required:
        - customer_details
      type: object
      properties:
        customer_details:
          $ref: "#/components/schemas/Customer"
        identity_details:
          $ref: "#/components/schemas/CustomerIdentityVerification"
      additionalProperties: false
    CustomerReviewResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        data:
          $ref: "#/components/schemas/CustomerReview"
      additionalProperties: false
    CustomerSortBy:
      enum:
        - name
        - created_at
      type: string
    CustomerStatus:
      enum:
        - pending
        - review
        - verified
        - inactive
        - rejected
      type: string
      default: verified
    CustomerVerificationDecision:
      enum:
        - verified
        - rejected
      type: string
      description: The final status of the customer review.
      default: verified
    CustomerSummary:
      required:
        - created_at
        - name
        - phone
        - updated_at
        - status
        - id
        - email
        - type
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for the customer.
          format: uuid
        name:
          type: string
          description: Full name for an individual customer or business name for a
            business customer.
        type:
          $ref: "#/components/schemas/CustomerType"
        email:
          type: string
          description: The customer's email address.
          format: email
        phone:
          type: string
          description: The customer's phone number in E.164 format.
          pattern: ^\+[1-9]\d{1,14}$
        external_id:
          type: string
          description: Unique identifier for the customer in your system.
          nullable: true
        status:
          $ref: "#/components/schemas/CustomerStatus"
        created_at:
          type: string
          description: Timestamp of when the customer record was created.
          format: date-time
        updated_at:
          type: string
          description: Timestamp of the most recent update to the customer record.
          format: date-time
      additionalProperties: false
    CustomerSummaryListResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/PageMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        data:
          type: array
          items:
            $ref: "#/components/schemas/CustomerSummary"
          description: Customers returned for this page.
      additionalProperties: false
    CustomerType:
      enum:
        - individual
        - business
      type: string
    UnmaskedCustomer:
      required:
        - created_at
        - name
        - phone
        - updated_at
        - status
        - id
        - email
        - type
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for the customer.
          format: uuid
        name:
          type: string
          description: Full name for an individual customer or business name for a
            business customer.
          example: Ron Swanson
        type:
          $ref: "#/components/schemas/CustomerType"
        email:
          type: string
          description: The customer's email address.
          format: email
          example: ron.swanson@pawnee.com
        phone:
          type: string
          description: The customer's phone number in E.164 format.
          example: "+12128675309"
          pattern: ^\+[1-9]\d{1,14}$
        external_id:
          type: string
          description: Unique identifier for the customer in your system.
          nullable: true
        status:
          $ref: "#/components/schemas/CustomerStatus"
        created_at:
          type: string
          description: Timestamp of when the customer record was created.
          format: date-time
        updated_at:
          type: string
          description: Timestamp of the most recent update to the customer record.
          format: date-time
        address:
          $ref: "#/components/schemas/CustomerAddress"
        compliance_profile:
          $ref: "#/components/schemas/UnmaskedComplianceProfile"
          nullable: true
        device:
          $ref: "#/components/schemas/CustomerDevice"
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Up to 20 user-defined key-value pairs associated with the customer.
          nullable: true
        config:
          $ref: "#/components/schemas/CustomerConfiguration"
      additionalProperties: false
    UnmaskedCustomerResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        data:
          $ref: "#/components/schemas/UnmaskedCustomer"
      additionalProperties: false
    Customer:
      required:
        - created_at
        - name
        - phone
        - updated_at
        - status
        - id
        - email
        - type
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for the customer.
          format: uuid
        name:
          type: string
          description: Full name for an individual customer or business name for a
            business customer.
          example: Ron Swanson
        type:
          $ref: "#/components/schemas/CustomerType"
        email:
          type: string
          description: The customer's email address.
          format: email
          example: ron.swanson@pawnee.com
        phone:
          type: string
          description: The customer's phone number in E.164 format.
          pattern: ^\+[1-9]\d{1,14}$
          example: "+12128675309"
        external_id:
          type: string
          description: Unique identifier for the customer in your system.
          nullable: true
        status:
          $ref: "#/components/schemas/CustomerStatus"
        created_at:
          type: string
          description: Timestamp of when the customer record was created.
          format: date-time
        updated_at:
          type: string
          description: Timestamp of the most recent update to the customer record.
          format: date-time
        address:
          $ref: "#/components/schemas/CustomerAddress"
          nullable: true
        compliance_profile:
          $ref: "#/components/schemas/ComplianceProfile"
          nullable: true
        device:
          $ref: "#/components/schemas/MaskedCustomerDevice"
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Up to 20 user-defined key-value pairs associated with the customer.
          nullable: true
        config:
          $ref: "#/components/schemas/CustomerConfiguration"
      additionalProperties: false
    CustomerResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        data:
          $ref: "#/components/schemas/Customer"
      additionalProperties: false
    CustomerDevice:
      required:
        - ip_address
      type: object
      properties:
        ip_address:
          minLength: 1
          type: string
          description: Customer IP address at profile creation. `0.0.0.0` represents an
            offline registration.
          format: ipv4
          example: 192.168.1.1
      additionalProperties: false
    MaskedCustomerDevice:
      required:
        - ip_address
      type: object
      properties:
        ip_address:
          minLength: 1
          type: string
          description: Masked IP address of the customer's device at the time of profile
            creation.
          example: "**.**.**.**"
      additionalProperties: false
    ApiError:
      required:
        - type
        - status
        - title
      type: object
      properties:
        status:
          type: integer
          description: HTTP status code for the error.
          format: int32
        type:
          type: string
          description: Identifier for the error type.
        title:
          type: string
          description: Short summary of the error.
        detail:
          type: string
          description: Explanation specific to this request.
          nullable: true
        items:
          type: array
          items:
            $ref: "#/components/schemas/ApiErrorDetail"
          description: Additional error details.
          nullable: true
      additionalProperties: false
    ApiErrorDetail:
      required:
        - detail
      type: object
      properties:
        reference:
          type: string
          description: Request field or error code associated with this detail.
          nullable: true
        detail:
          type: string
          description: Explanation of this error detail.
      additionalProperties: false
    ErrorResponse:
      required:
        - error
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        error:
          $ref: "#/components/schemas/ApiError"
      additionalProperties: false
    VerificationDecision:
      enum:
        - accept
        - reject
        - review
      type: string
    IdentityVerificationAlerts:
      type: object
      properties:
        decision:
          $ref: "#/components/schemas/VerificationDecision"
        codes:
          type: array
          items:
            type: string
          description: List of specific result codes from the consortium alert screening.
          nullable: true
        alerts:
          type: array
          items:
            type: string
          description: Any alerts or flags raised during the consortium alert screening.
          nullable: true
      additionalProperties: false
    IdentityVerificationBreakdown:
      type: object
      properties:
        decision:
          $ref: "#/components/schemas/VerificationDecision"
        codes:
          type: array
          items:
            type: string
          description: List of specific result codes from the fraud and risk screening.
          nullable: true
        risk_score:
          type: number
          description: Predicts the inherent risk associated with the customer for a given
            module. A higher score indicates a greater likelihood of fraud.
          format: double
          nullable: true
        correlation_score:
          type: number
          description: Represents the strength of the correlation between provided and
            known information. A higher score indicates a stronger correlation.
          format: double
          nullable: true
        correlation:
          $ref: "#/components/schemas/CorrelationBucket"
      additionalProperties: false
    IdentityVerificationWatchlist:
      type: object
      properties:
        decision:
          $ref: "#/components/schemas/VerificationDecision"
        codes:
          type: array
          items:
            type: string
          description: Result codes from Straddle watchlist screening.
          nullable: true
        matched:
          type: array
          items:
            type: string
          description: Names of watchlists with matches.
          nullable: true
        matches:
          type: array
          items:
            $ref: "#/components/schemas/IdentityVerificationWatchlistMatch"
          description: Details for matches found during watchlist screening.
          nullable: true
      additionalProperties: false
    CustomerKycVerification:
      required:
        - validations
      type: object
      properties:
        decision:
          $ref: "#/components/schemas/VerificationDecision"
        codes:
          type: array
          items:
            type: string
          description: Result codes from Know Your Customer (KYC) screening.
          nullable: true
        validations:
          type: object
          properties:
            first_name:
              type: boolean
              description: Whether the customer's first name passed validation.
            last_name:
              type: boolean
              description: Whether the customer's last name passed validation.
            address:
              type: boolean
              description: Whether the customer's address passed validation.
            city:
              type: boolean
              description: Whether the customer's city passed validation.
            state:
              type: boolean
              description: Whether the customer's state passed validation.
            zip:
              type: boolean
              description: Whether the customer's ZIP code passed validation.
            phone:
              type: boolean
              description: Whether the customer's phone passed validation.
            dob:
              type: boolean
              description: Whether the customer's date of birth passed validation.
            ssn:
              type: boolean
              description: Whether the customer's Social Security number passed validation.
            email:
              type: boolean
              description: Whether the customer's email passed validation.
          additionalProperties: false
          description: Results for each Know Your Customer (KYC) validation.
      additionalProperties: false
    SetCustomerVerificationDecisionRequest:
      required:
        - status
      type: object
      properties:
        status:
          $ref: "#/components/schemas/CustomerVerificationDecision"
      additionalProperties: false
    UnmaskedPaykey:
      required:
        - created_at
        - status
        - label
        - updated_at
        - id
        - source
        - paykey
        - config
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for the paykey.
          format: uuid
        customer_id:
          type: string
          description: Unique identifier for the customer associated with the paykey.
          format: uuid
          nullable: true
        label:
          type: string
          description: Human-readable label for the paykey.
        source:
          $ref: "#/components/schemas/PaykeySource"
        institution_name:
          type: string
          description: Name of the financial institution.
          example: Bank of America
          nullable: true
        status:
          $ref: "#/components/schemas/PaykeyStatus"
        status_details:
          $ref: "#/components/schemas/PaymentStatusDetails"
        expires_at:
          type: string
          description: Expiration date and time of the paykey, if applicable.
          format: date-time
          nullable: true
        created_at:
          type: string
          description: Timestamp of when the paykey was created.
          format: date-time
        updated_at:
          type: string
          description: Timestamp of the most recent update to the paykey.
          format: date-time
        paykey:
          type: string
          description: Full paykey value for creating payments. Store this value securely.
        bank_data:
          $ref: "#/components/schemas/UnmaskedPaykeyBankDetails"
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Up to 20 user-defined key-value pairs associated with the paykey.
          nullable: true
        config:
          $ref: "#/components/schemas/PaykeyConfiguration"
        balance:
          $ref: "#/components/schemas/PaykeyBalanceDetails"
        external_id:
          type: string
          description: Unique identifier for the paykey in your system.
          nullable: true
      additionalProperties: false
    UnmaskedPaykeyResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        data:
          $ref: "#/components/schemas/UnmaskedPaykey"
      additionalProperties: false
    UnmaskedPaykeyBankDetails:
      required:
        - account_type
        - routing_number
        - account_number
      type: object
      properties:
        routing_number:
          type: string
          description: Bank routing number.
          minLength: 9
          maxLength: 9
          example: "021000021"
        account_number:
          type: string
          description: Bank account number.
          example: "123456789"
        account_type:
          $ref: "#/components/schemas/AccountType"
      additionalProperties: false
    PaykeyBankDetails:
      required:
        - account_type
        - routing_number
        - account_number
      type: object
      properties:
        routing_number:
          type: string
          description: Bank routing number.
          minLength: 9
          maxLength: 9
          example: "021000021"
        account_number:
          type: string
          description: Masked bank account number.
          example: "****1234"
        account_type:
          $ref: "#/components/schemas/AccountType"
      additionalProperties: false
    PaykeySortBy:
      enum:
        - institution_name
        - expires_at
        - created_at
      type: string
    PaykeySource:
      enum:
        - bank_account
        - straddle
        - mx
        - plaid
        - tan
        - quiltt
      type: string
    PaykeyStatus:
      enum:
        - pending
        - active
        - inactive
        - rejected
        - review
        - blocked
      type: string
      default: active
    PaykeySummary:
      required:
        - created_at
        - status
        - label
        - updated_at
        - id
        - source
        - paykey
        - config
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for the paykey.
          format: uuid
        customer_id:
          type: string
          description: Unique identifier for the customer associated with the paykey.
          format: uuid
          nullable: true
        label:
          type: string
          description: Display label combining the bank name and masked account number.
          example: Bank of America ****1234
        source:
          $ref: "#/components/schemas/PaykeySource"
        institution_name:
          type: string
          description: Name of the financial institution.
          nullable: true
          example: Bank of America
        status:
          $ref: "#/components/schemas/PaykeyStatus"
        status_details:
          $ref: "#/components/schemas/PaymentStatusDetails"
        expires_at:
          type: string
          description: Expiration date and time of the paykey, if applicable.
          format: date-time
          nullable: true
        created_at:
          type: string
          description: Timestamp of when the paykey was created.
          format: date-time
        updated_at:
          type: string
          description: Timestamp of the most recent update to the paykey.
          format: date-time
        bank_data:
          $ref: "#/components/schemas/PaykeyBankDetails"
        paykey:
          type: string
          description: Masked paykey value.
        config:
          $ref: "#/components/schemas/PaykeyConfiguration"
        external_id:
          type: string
          description: Unique identifier for the paykey in your system.
          nullable: true
        unblock_eligible:
          type: boolean
          description: Whether the paykey is eligible for client-initiated unblocking.
            `true` only when the paykey is blocked by an `R29` return and has
            not been unblocked before. `false` for other blocked paykeys. `null`
            when the paykey is not blocked.
          nullable: true
      additionalProperties: false
    PaykeySummaryListResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/PageMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        data:
          type: array
          items:
            $ref: "#/components/schemas/PaykeySummary"
          description: The `data` field contains the paykeys on this page.
      additionalProperties: false
    Paykey:
      required:
        - created_at
        - status
        - label
        - updated_at
        - id
        - source
        - paykey
        - config
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for the paykey.
          format: uuid
        customer_id:
          type: string
          description: Unique identifier for the customer associated with the paykey.
          format: uuid
          nullable: true
        label:
          type: string
          description: Human-readable label for the paykey.
        source:
          $ref: "#/components/schemas/PaykeySource"
        institution_name:
          type: string
          description: Name of the financial institution.
          example: Bank of America
          nullable: true
        status:
          $ref: "#/components/schemas/PaykeyStatus"
        status_details:
          $ref: "#/components/schemas/PaymentStatusDetails"
        expires_at:
          type: string
          description: Expiration date and time of the paykey, if applicable.
          format: date-time
          nullable: true
        created_at:
          type: string
          description: Timestamp of when the paykey was created.
          format: date-time
        updated_at:
          type: string
          description: Timestamp of the most recent update to the paykey.
          format: date-time
        paykey:
          type: string
          description: Masked paykey value.
        bank_data:
          $ref: "#/components/schemas/PaykeyBankDetails"
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Up to 20 user-defined key-value pairs associated with the paykey.
          nullable: true
        config:
          $ref: "#/components/schemas/PaykeyConfiguration"
        balance:
          $ref: "#/components/schemas/PaykeyBalanceDetails"
        external_id:
          type: string
          description: Unique identifier for the paykey in your system.
          nullable: true
        unblock_eligible:
          type: boolean
          description: Whether the paykey is eligible for client-initiated unblocking.
            `true` only when the paykey is blocked by an `R29` return and has
            not been unblocked before. `false` for other blocked paykeys. `null`
            when the paykey is not blocked.
          nullable: true
      additionalProperties: false
    PaykeyResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        data:
          $ref: "#/components/schemas/Paykey"
      additionalProperties: false
    ResponseType:
      enum:
        - object
        - array
        - error
        - none
      type: string
      description: |-
        Shape of the response envelope.
        - `object` means `data` contains one JSON object.
        - `array` means `data` contains an array of JSON objects.
        - `error` means `error` contains the error details.
        - `none` means the response contains no data.
    SortOrder:
      enum:
        - asc
        - desc
      type: string
      description: Sort direction for the results.
      default: asc
    PaymentStatusDetails:
      required:
        - message
        - changed_at
        - reason
        - source
      type: object
      properties:
        message:
          type: string
          description: Human-readable status description.
          example: Payment successfully created and awaiting validation.
        reason:
          $ref: "#/components/schemas/PaymentStatusReason"
          description: Machine-readable reason for the status.
          example: OK
        source:
          $ref: "#/components/schemas/PaymentStatusSource"
          description: Source of the status change.
          example: system
        changed_at:
          type: string
          description: Timestamp when the status changed.
          format: date-time
        code:
          type: string
          description: Status code, when available.
          nullable: true
      additionalProperties: false
    UpdateCustomerRequest:
      required:
        - name
        - phone
        - status
        - device
        - email
      type: object
      properties:
        name:
          type: string
          description: Full name for an individual customer or business name for a
            business customer.
        email:
          type: string
          description: Customer email address.
          format: email
        address:
          $ref: "#/components/schemas/CustomerAddress"
          description: Customer postal address. When provided, the object must include all
            required fields.
        phone:
          type: string
          description: Customer phone number in E.164 format.
          pattern: ^\+[1-9]\d{1,14}$
        compliance_profile:
          $ref: "#/components/schemas/UnmaskedComplianceProfile"
          nullable: true
        external_id:
          type: string
          description: Unique identifier for the customer in your system.
          nullable: true
        device:
          $ref: "#/components/schemas/CustomerDevice"
        status:
          $ref: "#/components/schemas/CustomerStatus"
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Up to 20 user-defined key-value pairs associated with the customer.
          nullable: true
      additionalProperties: false
    BalanceCheckMode:
      enum:
        - required
        - enabled
        - disabled
      type: string
    ChargeConfiguration:
      required:
        - balance_check
      type: object
      properties:
        balance_check:
          $ref: "#/components/schemas/BalanceCheckMode"
          description: Balance check mode to use before processing the charge.
          default: enabled
        sandbox_outcome:
          $ref: "#/components/schemas/SimulatedPaymentOutcome"
        auto_hold:
          type: boolean
          description: Whether to place the charge on hold automatically after creation.
          nullable: true
        auto_hold_message:
          type: string
          description: Reason for placing the charge on hold automatically.
          nullable: true
      additionalProperties: false
    Charge:
      required:
        - consent_type
        - created_at
        - config
        - currency
        - status
        - updated_at
        - device
        - status_history
        - id
        - paykey
        - status_details
        - amount
        - description
        - payment_date
        - external_id
        - funding_ids
        - trace_ids
        - has_refund
        - has_resubmit
        - is_resubmit
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for this charge.
          format: uuid
        paykey:
          type: string
          description: The masked paykey token used for this charge.
        description:
          type: string
          description: A human-readable description of the charge.
          example: Monthly subscription fee
          nullable: true
        payment_rail:
          $ref: "#/components/schemas/PaymentRail"
          description: Payment rail used to process the charge.
        paykey_details:
          $ref: "#/components/schemas/PaykeyDetails"
          description: Information about the paykey used for the charge.
        customer_details:
          $ref: "#/components/schemas/CustomerDetails"
          description: Information about the customer associated with the charge.
        amount:
          type: integer
          description: Amount in cents.
          format: int32
          example: 10000
        currency:
          type: string
          description: Currency code. Only `USD` is supported.
          default: USD
        payment_date:
          type: string
          description: Date when Straddle submits the charge for processing.
          format: date
        consent_type:
          $ref: "#/components/schemas/ConsentType"
          description: How the customer authorized the charge. `internet` covers online
            and mobile authorization. `signed` covers written or PDF-signed
            agreements.
        device:
          $ref: "#/components/schemas/MaskedPaymentDevice"
          description: Device used when the customer authorized the charge.
        external_id:
          type: string
          description: Your unique identifier for this charge, used to correlate with your
            internal records.
        config:
          $ref: "#/components/schemas/ChargeConfiguration"
          description: Configuration options for the charge.
        created_at:
          type: string
          description: Timestamp when this charge was created.
          format: date-time
          nullable: true
        updated_at:
          type: string
          description: Timestamp when this charge was last updated.
          format: date-time
          nullable: true
        processed_at:
          type: string
          description: Timestamp when this charge was submitted to the payment network.
            Null until processed.
          format: date-time
          nullable: true
        effective_at:
          type: string
          description: Timestamp when funds were settled. Null until settlement is
            confirmed.
          format: date-time
          nullable: true
        status:
          $ref: "#/components/schemas/PaymentStatus"
          description: The current status of the charge.
        status_details:
          $ref: "#/components/schemas/PaymentStatusDetails"
          description: Reason, source, and message for the most recent charge status change.
        status_history:
          type: array
          items:
            $ref: "#/components/schemas/PaymentStatusHistory"
            description: One status change for the charge.
          description: Complete ordered history of all status changes for this charge.
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Key-value metadata stored with this charge.
          nullable: true
        funding_ids:
          type: array
          items:
            type: string
            format: uuid
          description: IDs of the funding events that included this charge.
        trace_ids:
          type: object
          additionalProperties:
            type: string
          description: Trace identifiers from the payment network. Keys depend on the
            payment rail.
        related_payments:
          type: array
          items:
            $ref: "#/components/schemas/RelatedPayment"
          description: Related payments and their relationship to this charge.
          nullable: true
        has_refund:
          type: boolean
          description: Whether an associated payout has refunded this charge.
        is_resubmit:
          type: boolean
          description: Whether this charge resubmits an original charge.
        has_resubmit:
          type: boolean
          description: Whether this charge has been resubmitted.
        documents:
          type: array
          items:
            $ref: "#/components/schemas/PaymentAuthorizationProof"
          description: Authorization documents for this charge, ordered by upload time.
          nullable: true
      additionalProperties: false
    ChargeResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        data:
          $ref: "#/components/schemas/Charge"
      additionalProperties: false
    ConsentType:
      enum:
        - internet
        - signed
      type: string
      description: How the customer authorized the charge. `internet` covers online
        and mobile authorization. `signed` covers written or PDF-signed
        agreements.
    CreateChargeRequest:
      required:
        - consent_type
        - config
        - currency
        - device
        - paykey
        - amount
        - description
        - payment_date
        - external_id
      type: object
      properties:
        paykey:
          type: string
          description: The paykey token that identifies the customer's bank account.
        description:
          type: string
          description: Description shown on the customer's bank statement where supported.
          example: Monthly subscription fee
          nullable: true
        amount:
          type: integer
          description: Amount in cents.
          format: int32
          example: 10000
        currency:
          type: string
          description: Currency code. Only `USD` is supported.
          default: USD
        payment_date:
          type: string
          description: Date when Straddle submits the charge for processing.
          format: date
        consent_type:
          $ref: "#/components/schemas/ConsentType"
          description: How the customer authorized the charge. `internet` covers online
            and mobile authorization. `signed` covers written or PDF-signed
            agreements.
        device:
          $ref: "#/components/schemas/PaymentDevice"
        external_id:
          type: string
          description: Your unique identifier for the charge. Must be unique across charges.
        config:
          $ref: "#/components/schemas/ChargeConfiguration"
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Up to 20 user-defined string key-value pairs.
          nullable: true
      additionalProperties: false
    CreatePayoutRequest:
      required:
        - currency
        - device
        - paykey
        - amount
        - description
        - payment_date
        - external_id
      type: object
      properties:
        paykey:
          type: string
          description: The paykey token that identifies the customer's bank account.
        description:
          type: string
          description: Description shown on the customer's bank statement where supported.
          example: Vendor invoice payment
          nullable: true
        amount:
          type: integer
          description: Amount in cents.
          format: int32
          example: 10000
        currency:
          type: string
          description: Currency code. Only `USD` is supported.
          default: USD
        payment_date:
          type: string
          description: Date when Straddle submits the payout for processing.
          format: date
        device:
          $ref: "#/components/schemas/PaymentDevice"
          description: Device used when the customer authorized the payout.
        external_id:
          type: string
          description: Your unique identifier for the payout. Must be unique across payouts.
        config:
          $ref: "#/components/schemas/PayoutConfiguration"
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Up to 20 user-defined string key-value pairs.
          nullable: true
      additionalProperties: false
    CustomerDetails:
      required:
        - name
        - phone
        - id
        - email
        - customer_type
      type: object
      description: Information about the customer associated with the charge or payout.
      properties:
        id:
          type: string
          description: Unique identifier for the customer.
          format: uuid
        name:
          type: string
          description: Customer's full name or business name.
          example: Ron Swanson
        customer_type:
          $ref: "#/components/schemas/CustomerType"
          description: Whether the customer is an individual or a business.
        email:
          type: string
          description: Customer's email address.
          example: ron@swanson.com
        phone:
          type: string
          description: Customer's phone number in E.164 format.
          example: "+1234567890"
      additionalProperties: false
    PaymentDevice:
      required:
        - ip_address
      type: object
      properties:
        ip_address:
          type: string
          description: The IP address of the device used when the customer authorized the
            charge or payout. Use `0.0.0.0` to represent an offline consent
            interaction.
          format: ipv4
          example: 192.168.1.1
      additionalProperties: false
    MaskedPaymentDevice:
      required:
        - ip_address
      type: object
      properties:
        ip_address:
          type: string
          description: Masked IP address of the device used when the customer authorized
            the charge or payout.
          example: "**.**.**.**"
      additionalProperties: false
    FundingEventSortBy:
      enum:
        - transfer_date
        - id
        - amount
      type: string
      description: Field used to sort funding events.
    FundingEventSummary:
      required:
        - direction
        - event_type
        - id
        - transfer_date
        - payment_count
        - amount
        - trace_numbers
        - created_at
        - trace_ids
        - updated_at
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for this funding event.
          format: uuid
        amount:
          type: integer
          description: Total funding event amount in the smallest currency unit. For
            example, `1000` is $10.00 USD.
          format: int32
          example: 100000
        direction:
          $ref: "#/components/schemas/TransferDirection"
        event_type:
          $ref: "#/components/schemas/FundingEventType"
        trace_number:
          type: string
          description: The trace number of the funding event.
          nullable: true
        payment_count:
          type: integer
          description: Number of payments included in this funding event.
          format: int32
        transfer_date:
          type: string
          description: The date the funds transfer was initiated.
          format: date
        trace_numbers:
          type: array
          items:
            type: string
          description: Network trace numbers associated with payments in this funding event.
        created_at:
          type: string
          description: Timestamp when this funding event was created.
          format: date-time
        updated_at:
          type: string
          description: Timestamp when this funding event was last updated.
          format: date-time
        trace_ids:
          type: object
          additionalProperties:
            type: string
          description: Network-level trace identifiers assigned during processing. Keys
            vary by payment rail.
        status:
          $ref: "#/components/schemas/PaymentStatus"
          description: Current status of this funding event.
        status_details:
          $ref: "#/components/schemas/PaymentStatusDetails"
          description: Reason, source, and message for the most recent status change.
      additionalProperties: false
    FundingEventSummaryListResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/PageMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        data:
          type: array
          items:
            $ref: "#/components/schemas/FundingEventSummary"
          description: Funding events returned for this page.
      additionalProperties: false
    FundingEventType:
      enum:
        - charge_deposit
        - charge_reversal
        - payout_return
        - payout_withdrawal
      type: string
      description: Reason for the funding event. `charge_deposit` settles collected
        charges to the linked bank account. `charge_reversal` withdraws funds
        for reversed charges. `payout_withdrawal` withdraws funds for payouts.
        `payout_return` deposits returned payout funds.
    PaykeyDetails:
      required:
        - id
        - label
        - customer_id
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for the paykey.
          format: uuid
        customer_id:
          type: string
          description: Unique identifier for the customer associated with the paykey.
          format: uuid
        label:
          type: string
          description: Display label combining the bank name and masked account number.
          example: Bank of America ****1234
        balance:
          type: integer
          description: The most recent available balance in the smallest currency unit, if
            a balance check was performed.
          format: int32
          nullable: true
          example: 10000
      additionalProperties: false
    PaymentRail:
      enum:
        - ach
      type: string
      description: The payment rail used for the charge or payout.
    PaymentSortBy:
      enum:
        - created_at
        - payment_date
        - effective_at
        - id
        - amount
        - updated_at
      type: string
      description: The field to sort the results by.
      default: id
    PaymentStatus:
      enum:
        - created
        - scheduled
        - failed
        - cancelled
        - on_hold
        - pending
        - paid
        - reversed
        - validating
      type: string
      description: The current status of the `charge` or `payout`.
      default: created
    PaymentSummary:
      required:
        - created_at
        - currency
        - status
        - updated_at
        - id
        - paykey
        - status_details
        - payment_type
        - description
        - amount
        - payment_date
        - external_id
        - funding_ids
        - trace_ids
        - has_refund
        - has_resubmit
        - is_refund
        - is_resubmit
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for this charge or payout.
          format: uuid
        payment_type:
          $ref: "#/components/schemas/PaymentType"
          description: Whether this payment is a charge or payout.
        payment_date:
          type: string
          description: Date when Straddle submits the payment for processing.
          format: date
        effective_at:
          type: string
          description: Timestamp when funds settled. Null until settlement is confirmed.
          format: date-time
          nullable: true
        description:
          type: string
          description: Human-readable description of the payment.
          example: Invoice payment for 100 widgets
          nullable: true
        external_id:
          type: string
          description: Your unique identifier for the charge or payout.
        amount:
          type: integer
          description: Amount in cents.
          format: int32
          example: 10000
        currency:
          type: string
          description: Currency code. Only `USD` is supported.
          default: USD
        customer_details:
          $ref: "#/components/schemas/CustomerDetails"
        paykey:
          type: string
          description: Masked paykey token used for the charge or payout.
        paykey_details:
          $ref: "#/components/schemas/PaykeyDetails"
          description: Details of the paykey used for the charge or payout.
        status:
          $ref: "#/components/schemas/PaymentStatus"
          description: Current status of the charge or payout.
        status_details:
          $ref: "#/components/schemas/PaymentStatusDetails"
          description: Reason, source, and message for the most recent status change.
        funding_id:
          type: string
          description: Unique identifier for the funding event associated with the
            `charge` or `payout`.
          format: uuid
          nullable: true
        created_at:
          type: string
          description: Timestamp when the charge or payout was created.
          format: date-time
        updated_at:
          type: string
          description: Timestamp when the charge or payout was last updated.
          format: date-time
        funding_ids:
          type: array
          items:
            type: string
            format: uuid
          description: IDs of the funding events that included this payment.
        trace_ids:
          type: object
          additionalProperties:
            type: string
          description: Network-level trace identifiers assigned during processing. Keys
            vary by payment rail.
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Key-value metadata for the payment. Included only when
            `include_metadata` is true.
          nullable: true
        related_payments:
          type: array
          items:
            $ref: "#/components/schemas/RelatedPayment"
          description: Related payments and their relationship to this charge or payout.
          nullable: true
        is_refund:
          type: boolean
          description: Whether this payment is a payout that refunds an original charge.
        has_refund:
          type: boolean
          description: Whether this payment is a charge refunded by an associated payout.
        is_resubmit:
          type: boolean
          description: Whether this payment resubmits an original payment.
        has_resubmit:
          type: boolean
          description: Whether this payment has been resubmitted.
      additionalProperties: false
    PaymentSummaryListResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/PageMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        data:
          type: array
          items:
            $ref: "#/components/schemas/PaymentSummary"
          description: Payments returned for this page.
      additionalProperties: false
    PaymentType:
      enum:
        - charge
        - payout
      type: string
      description: The type of payment.
    PayoutConfiguration:
      type: object
      additionalProperties: false
      properties:
        sandbox_outcome:
          $ref: "#/components/schemas/SimulatedPaymentOutcome"
        auto_hold:
          type: boolean
          description: Whether to place the payout on hold automatically after creation.
          nullable: true
        auto_hold_message:
          type: string
          description: Reason for placing the payout on hold automatically.
          nullable: true
    Payout:
      required:
        - config
        - currency
        - status
        - status_history
        - device
        - id
        - paykey
        - status_details
        - amount
        - description
        - payment_date
        - external_id
        - funding_ids
        - trace_ids
        - has_resubmit
        - is_refund
        - is_resubmit
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for this payout.
          format: uuid
        paykey:
          type: string
          description: The masked paykey token used for this payout.
        description:
          type: string
          description: A human-readable description of the payout.
          nullable: true
        payment_rail:
          $ref: "#/components/schemas/PaymentRail"
          description: Payment rail used to process the payout.
        customer_details:
          $ref: "#/components/schemas/CustomerDetails"
          description: Information about the customer associated with the payout.
        paykey_details:
          $ref: "#/components/schemas/PaykeyDetails"
          description: Information about the paykey used for the payout.
        amount:
          type: integer
          description: Amount in cents.
          format: int32
          example: 10000
        currency:
          type: string
          description: Currency code. Only `USD` is supported.
          default: USD
        payment_date:
          type: string
          description: Date when Straddle submits the payout for processing.
          format: date
        device:
          $ref: "#/components/schemas/MaskedPaymentDevice"
          description: Device used when the customer authorized the payout.
        external_id:
          type: string
          description: Your unique identifier for this payout, used to correlate with your
            internal records.
        config:
          $ref: "#/components/schemas/PayoutConfiguration"
          description: Configuration for the payout.
        created_at:
          type: string
          description: Timestamp when this payout was created.
          format: date-time
          nullable: true
        updated_at:
          type: string
          description: Timestamp when this payout was last updated.
          format: date-time
          nullable: true
        processed_at:
          type: string
          description: Timestamp when this payout was submitted to the payment network.
            Null until processed.
          format: date-time
          nullable: true
        effective_at:
          type: string
          description: Timestamp when funds were settled. Null until settlement is
            confirmed.
          format: date-time
          nullable: true
        status:
          $ref: "#/components/schemas/PaymentStatus"
          description: The current status of the payout.
          example: created
        status_details:
          $ref: "#/components/schemas/PaymentStatusDetails"
          description: Reason, source, and message for the most recent payout status change.
        status_history:
          type: array
          items:
            $ref: "#/components/schemas/PaymentStatusHistory"
          description: Complete ordered history of all status changes for this payout.
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Key-value metadata stored with this payout.
          nullable: true
        funding_ids:
          type: array
          items:
            type: string
            format: uuid
          description: IDs of the funding events that included this payout.
        trace_ids:
          type: object
          additionalProperties:
            type: string
          description: Trace identifiers from the payment network. Keys depend on the
            payment rail.
        related_payments:
          type: array
          items:
            $ref: "#/components/schemas/RelatedPayment"
          description: Related payments and their relationship to this payout.
          nullable: true
        is_refund:
          type: boolean
          description: Whether this payout refunds an original charge.
        is_resubmit:
          type: boolean
          description: Whether this payout resubmits an original payout.
        has_resubmit:
          type: boolean
          description: Whether this payout has been resubmitted.
        documents:
          type: array
          items:
            $ref: "#/components/schemas/PaymentAuthorizationProof"
          description: Authorization documents for this payout, ordered by upload time.
          nullable: true
      additionalProperties: false
    PayoutResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        data:
          $ref: "#/components/schemas/Payout"
      additionalProperties: false
    PaymentStatusHistory:
      required:
        - status
        - message
        - source
        - changed_at
        - reason
      type: object
      properties:
        reason:
          $ref: "#/components/schemas/PaymentStatusReason"
          description: Machine-readable reason for the status.
        source:
          $ref: "#/components/schemas/PaymentStatusSource"
          description: Source of the status change.
        message:
          type: string
          description: Human-readable status description.
          example: Payment successfully created and awaiting validation.
        code:
          type: string
          description: Status code, when available.
          nullable: true
        changed_at:
          type: string
          description: Timestamp when the status changed.
          format: date-time
        status:
          $ref: "#/components/schemas/PaymentStatus"
      additionalProperties: false
    PaymentStatusReason:
      enum:
        - insufficient_funds
        - closed_bank_account
        - invalid_bank_account
        - invalid_routing
        - disputed
        - payment_stopped
        - owner_deceased
        - frozen_bank_account
        - risk_review
        - fraudulent
        - duplicate_entry
        - invalid_paykey
        - payment_blocked
        - amount_too_large
        - too_many_attempts
        - internal_system_error
        - user_request
        - ok
        - other_network_return
        - payout_refused
        - cancel_request
        - failed_verification
        - require_review
        - blocked_by_system
        - watchtower_review
        - validating
        - auto_hold
      type: string
    PaymentStatusSource:
      enum:
        - watchtower
        - bank_decline
        - customer_dispute
        - user_action
        - system
      type: string
    TransferDirection:
      enum:
        - deposit
        - withdrawal
      type: string
      description: Transfer direction relative to the linked bank account. `deposit`
        moves funds into the account, and `withdrawal` moves funds out.
    UpdateChargeStatusRequest:
      type: object
      properties:
        reason:
          type: string
          description: Message explaining the charge status change.
          nullable: true
      additionalProperties: false
    UpdateChargeRequest:
      required:
        - description
        - payment_date
        - amount
      type: object
      properties:
        description:
          type: string
          description: Updated description for the charge.
          example: Monthly subscription fee
          nullable: true
        amount:
          type: integer
          description: Amount in cents.
          format: int32
          example: 10000
        payment_date:
          type: string
          description: New date for Straddle to submit the charge for processing.
          format: date
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Replacement metadata for the charge. Up to 20 user-defined string
            key-value pairs.
          nullable: true
      additionalProperties: false
    UpdatePayoutStatusRequest:
      required:
        - reason
      type: object
      properties:
        reason:
          type: string
          description: Message explaining the payout status change.
      additionalProperties: false
    UpdatePayoutRequest:
      required:
        - description
        - payment_date
        - amount
      type: object
      properties:
        description:
          type: string
          description: Updated description for the payout.
          nullable: true
        amount:
          type: integer
          description: Amount in cents.
          format: int32
          example: 10000
        payment_date:
          type: string
          description: New date for Straddle to submit the payout for processing.
          format: date
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Replacement metadata for the payout. Up to 20 user-defined string
            key-value pairs.
          nullable: true
      additionalProperties: false
    RevealedPaykey:
      required:
        - created_at
        - status
        - label
        - updated_at
        - id
        - source
        - paykey
        - config
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for the paykey.
          format: uuid
        paykey:
          type: string
          description: Full paykey value for creating payments. Store this value securely.
        customer_id:
          type: string
          description: Unique identifier for the customer associated with the paykey.
          format: uuid
          nullable: true
        label:
          type: string
          description: Display label combining the bank name and masked account number.
          example: Bank of America ****1234
        source:
          $ref: "#/components/schemas/PaykeySource"
          example: straddle
        institution_name:
          type: string
          description: Name of the financial institution.
          nullable: true
          example: Bank of America
        status:
          $ref: "#/components/schemas/PaykeyStatus"
        status_details:
          $ref: "#/components/schemas/PaymentStatusDetails"
        expires_at:
          type: string
          description: Expiration date and time of the paykey, if applicable.
          format: date-time
          nullable: true
        created_at:
          type: string
          description: Timestamp of when the paykey was created.
          format: date-time
        updated_at:
          type: string
          description: Timestamp of the most recent update to the paykey.
          format: date-time
        bank_data:
          $ref: "#/components/schemas/PaykeyBankDetails"
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Up to 20 user-defined key-value pairs associated with the paykey.
          nullable: true
        config:
          $ref: "#/components/schemas/PaykeyConfiguration"
        balance:
          $ref: "#/components/schemas/PaykeyBalanceDetails"
        external_id:
          type: string
          description: Unique identifier for the paykey in your system.
          nullable: true
      additionalProperties: false
    RevealedPaykeyResponse:
      required:
        - data
        - response_type
        - meta
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        data:
          $ref: "#/components/schemas/RevealedPaykey"
      additionalProperties: false
    CorrelationBucket:
      enum:
        - low_confidence
        - potential_match
        - likely_match
        - high_confidence
      type: string
    IdentityVerificationWatchlistMatch:
      required:
        - correlation
        - list_name
        - urls
        - match_fields
      type: object
      properties:
        list_name:
          type: string
          description: Name of the watchlist that contains the matching record.
        urls:
          type: array
          items:
            type: string
          description: Source URLs associated with the match.
        match_fields:
          type: array
          items:
            type: string
          description: Customer fields that match the watchlist record.
        correlation:
          $ref: "#/components/schemas/CorrelationBucket"
      additionalProperties: false
    CreateLinkedBankAccountRequest:
      required:
        - bank_account
      type: object
      properties:
        account_id:
          type: string
          description: ID of the account that will own the linked bank account. Omit this
            field to assign ownership to the platform in the authenticated
            request context.
          format: uuid
          nullable: true
        bank_account:
          $ref: "#/components/schemas/LinkedBankAccountDetailsRequest"
        metadata:
          type: object
          additionalProperties:
            type: string
            nullable: true
          description: Up to 20 user-defined key-value pairs.
          nullable: true
        platform_id:
          type: string
          description: ID of the platform to associate with the linked bank account.
          format: uuid
          nullable: true
        purposes:
          type: array
          items:
            enum:
              - charges
              - payouts
              - billing
            type: string
          description: Payment purposes for the linked bank account. Defaults to
            `charges`, `payouts`, and `billing`.
          nullable: true
        description:
          type: string
          description: Your description for the linked bank account.
          nullable: true
      additionalProperties: false
    UnmaskedRepresentativeResponse:
      required:
        - data
        - meta
        - response_type
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          enum:
            - object
            - array
            - error
            - none
          type: string
          description: |-
            Indicates how the response content is structured.
            - `object` means `data` contains one JSON object.
            - `array` means `data` contains an array of objects.
            - `error` means `error` contains error details.
            - `none` means the response has no data.
        data:
          $ref: "#/components/schemas/UnmaskedRepresentative"
      additionalProperties: false
    UnmaskedRepresentative:
      required:
        - account_id
        - created_at
        - dob
        - email
        - first_name
        - id
        - last_name
        - mobile_number
        - relationship
        - ssn_last4
        - status
        - status_detail
        - updated_at
      type: object
      properties:
        id:
          type: string
          description: Straddle's unique ID for the representative.
          format: uuid
        account_id:
          type: string
          description: ID of the account associated with the representative.
          format: uuid
        user_id:
          type: string
          description: ID of the Straddle user linked to the representative, if any.
          format: uuid
          nullable: true
        status:
          enum:
            - created
            - onboarding
            - active
            - rejected
            - inactive
          type: string
          description: Status of the representative.
        status_detail:
          $ref: "#/components/schemas/RepresentativeStatusDetail"
        first_name:
          type: string
          description: Representative's first name.
        last_name:
          type: string
          description: Representative's last name.
        dob:
          type: string
          description: Representative's date of birth in `YYYY-MM-DD` format.
          format: date
        email:
          type: string
          description: Representative's email address.
        mobile_number:
          type: string
          description: Representative's mobile phone number.
        relationship:
          $ref: "#/components/schemas/RepresentativeRelationship"
        external_id:
          type: string
          description: Your unique ID for the representative.
          nullable: true
        created_at:
          type: string
          description: Date and time when Straddle created the representative.
          format: date-time
        updated_at:
          type: string
          description: Date and time of the most recent representative update.
          format: date-time
        ssn_last4:
          type: string
          description: Last four digits of the representative's Social Security number.
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Up to 20 user-defined key-value pairs.
          nullable: true
      additionalProperties: false
    BusinessCustomerRepresentative:
      required:
        - name
      type: object
      properties:
        name:
          type: string
          description: Full name of the representative.
        email:
          type: string
          nullable: true
          description: Email address of the representative.
        phone:
          type: string
          nullable: true
          description: Phone number of the representative.
      additionalProperties: false
    ReputationInsights:
      type: object
      properties:
        ach_fraud_transactions_count:
          type: integer
          format: int32
          nullable: true
          description: Number of fraudulent ACH transactions.
        ach_fraud_transactions_total_amount:
          type: number
          format: double
          nullable: true
          description: Total amount of fraudulent ACH transactions.
        ach_fraud_transactions_dates:
          type: array
          items:
            type: string
            format: date
          nullable: true
          description: Dates when fraudulent ACH transactions occurred.
        ach_returned_transactions_count:
          type: integer
          format: int32
          nullable: true
          description: Number of returned ACH transactions.
        ach_returned_transactions_total_amount:
          type: number
          format: double
          nullable: true
          description: Total amount of returned ACH transactions.
        ach_returned_transactions_dates:
          type: array
          items:
            type: string
            format: date
          nullable: true
          description: Dates when ACH transactions were returned.
        card_fraud_transactions_count:
          type: integer
          format: int32
          nullable: true
          description: Number of fraudulent card transactions.
        card_fraud_transactions_total_amount:
          type: number
          format: double
          nullable: true
          description: Total amount of fraudulent card transactions.
        card_fraud_transactions_dates:
          type: array
          items:
            type: string
            format: date
          nullable: true
          description: Dates when fraudulent card transactions occurred.
        card_disputed_transactions_count:
          type: integer
          format: int32
          nullable: true
          description: Number of disputed card transactions.
        card_disputed_transactions_total_amount:
          type: number
          format: double
          nullable: true
          description: Total amount of disputed card transactions.
        card_disputed_transactions_dates:
          type: array
          items:
            type: string
            format: date
          nullable: true
          description: Dates when card transactions were disputed.
        card_stopped_transactions_count:
          type: integer
          format: int32
          nullable: true
          description: Number of stopped card transactions.
        card_stopped_transactions_dates:
          type: array
          items:
            type: string
            format: date
          nullable: true
          description: Dates when card transactions were stopped.
        accounts_count:
          type: integer
          format: int32
          nullable: true
          description: Number of accounts associated with the identity.
        accounts_active_count:
          type: integer
          format: int32
          nullable: true
          description: Number of active accounts associated with the identity.
        accounts_closed_count:
          type: integer
          format: int32
          nullable: true
          description: Number of closed accounts associated with the identity.
        accounts_closed_dates:
          type: array
          items:
            type: string
            format: date
          nullable: true
          description: Dates when accounts associated with the identity were closed.
        accounts_fraud_count:
          type: integer
          format: int32
          nullable: true
          description: Number of accounts associated with fraud.
        accounts_fraud_loss_total_amount:
          type: number
          format: double
          nullable: true
          description: Total fraud loss associated with the accounts.
        accounts_fraud_labeled_dates:
          type: array
          items:
            type: string
            format: date
          nullable: true
          description: Dates when accounts were labeled as fraudulent.
        applications_count:
          type: integer
          format: int32
          nullable: true
          description: Number of applications associated with the identity.
        applications_dates:
          type: array
          items:
            type: string
            format: date
          nullable: true
          description: Dates when applications associated with the identity were submitted.
        applications_approved_count:
          type: integer
          format: int32
          nullable: true
          description: Number of approved applications associated with the identity.
        applications_declined_count:
          type: integer
          format: int32
          nullable: true
          description: Number of declined applications associated with the identity.
        applications_fraud_count:
          type: integer
          format: int32
          nullable: true
          description: Number of applications associated with fraud.
        user_institution_count:
          type: integer
          format: int32
          nullable: true
          description: Number of financial institutions associated with the identity.
        user_dob_count:
          type: integer
          format: int32
          nullable: true
          description: Number of dates of birth associated with the identity.
        user_mobile_count:
          type: integer
          format: int32
          nullable: true
          description: Number of mobile numbers associated with the identity.
        user_email_count:
          type: integer
          format: int32
          nullable: true
          description: Number of email addresses associated with the identity.
        user_address_count:
          type: integer
          format: int32
          nullable: true
          description: Number of addresses associated with the identity.
        user_active_profile_count:
          type: integer
          format: int32
          nullable: true
          description: Number of active profiles associated with the identity.
        user_closed_profile_count:
          type: integer
          format: int32
          nullable: true
          description: Number of closed profiles associated with the identity.
      additionalProperties: false
    ReputationCheck:
      type: object
      properties:
        decision:
          $ref: "#/components/schemas/VerificationDecision"
        codes:
          type: array
          items:
            type: string
          description: Specific codes related to the Straddle reputation screening results.
          nullable: true
        risk_score:
          type: number
          format: double
          nullable: true
          description: Risk score produced by the reputation check.
        insights:
          $ref: "#/components/schemas/ReputationInsights"
      additionalProperties: false
    UnmaskedCharge:
      required:
        - amount
        - config
        - consent_type
        - created_at
        - currency
        - description
        - device
        - external_id
        - funding_ids
        - id
        - paykey
        - payment_date
        - status
        - status_details
        - status_history
        - updated_at
        - trace_ids
        - has_refund
        - has_resubmit
        - is_resubmit
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for this charge.
          format: uuid
        description:
          type: string
          description: A human-readable description of the charge.
          nullable: true
        payment_rail:
          $ref: "#/components/schemas/PaymentRail"
        paykey_details:
          $ref: "#/components/schemas/PaykeyDetails"
        customer_details:
          $ref: "#/components/schemas/CustomerDetails"
        amount:
          type: integer
          description: Amount in cents.
          format: int32
        currency:
          type: string
          description: Currency code. Only `USD` is supported.
        payment_date:
          type: string
          description: Date when Straddle submits the charge for processing.
          format: date
        consent_type:
          $ref: "#/components/schemas/ConsentType"
        device:
          $ref: "#/components/schemas/PaymentDevice"
        external_id:
          type: string
          description: Your unique identifier for this charge, used to correlate with your
            internal records.
        config:
          $ref: "#/components/schemas/ChargeConfiguration"
        created_at:
          type: string
          description: Timestamp when this charge was created.
          format: date-time
        updated_at:
          type: string
          description: Timestamp when this charge was last updated.
          format: date-time
        processed_at:
          type: string
          description: Timestamp when this charge was submitted to the payment network.
            Null until processed.
          format: date-time
          nullable: true
        effective_at:
          type: string
          description: Timestamp when funds were settled. Null until settlement is
            confirmed.
          format: date-time
          nullable: true
        status:
          $ref: "#/components/schemas/PaymentStatus"
        status_details:
          $ref: "#/components/schemas/PaymentStatusDetails"
        status_history:
          type: array
          items:
            $ref: "#/components/schemas/PaymentStatusHistory"
          description: Complete ordered history of all status changes for this charge.
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Key-value metadata stored with this charge.
          nullable: true
        funding_ids:
          type: array
          items:
            type: string
            format: uuid
          description: IDs of the funding events that included this charge.
        paykey:
          type: string
          description: Unmasked paykey token used for this charge.
        trace_ids:
          type: object
          additionalProperties:
            type: string
          description: Trace identifiers from the payment network. Keys depend on the
            payment rail.
        related_payments:
          type: array
          items:
            $ref: "#/components/schemas/RelatedPayment"
          description: Related payments and their relationship to this charge.
          nullable: true
        has_refund:
          type: boolean
          description: Whether an associated payout has refunded this charge.
        is_resubmit:
          type: boolean
          description: Whether this charge resubmits an original charge.
        has_resubmit:
          type: boolean
          description: Whether this charge has been resubmitted.
        documents:
          type: array
          items:
            $ref: "#/components/schemas/PaymentAuthorizationProof"
          description: Authorization documents for this charge, ordered by upload time.
          nullable: true
      additionalProperties: false
    UnmaskedChargeResponse:
      required:
        - data
        - meta
        - response_type
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        data:
          $ref: "#/components/schemas/UnmaskedCharge"
      additionalProperties: false
    UnmaskedPayout:
      required:
        - amount
        - config
        - currency
        - description
        - device
        - external_id
        - funding_ids
        - id
        - paykey
        - payment_date
        - status
        - status_details
        - status_history
        - trace_ids
        - has_resubmit
        - is_refund
        - is_resubmit
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for this payout.
          format: uuid
        description:
          type: string
          description: A human-readable description of the payout.
          nullable: true
        payment_rail:
          $ref: "#/components/schemas/PaymentRail"
        customer_details:
          $ref: "#/components/schemas/CustomerDetails"
        paykey_details:
          $ref: "#/components/schemas/PaykeyDetails"
        amount:
          type: integer
          description: Amount in cents.
          format: int32
        currency:
          type: string
          description: Currency code. Only `USD` is supported.
        payment_date:
          type: string
          description: Date when Straddle submits the payout for processing.
          format: date
        device:
          $ref: "#/components/schemas/PaymentDevice"
        external_id:
          type: string
          description: Your unique identifier for this payout, used to correlate with your
            internal records.
        config:
          $ref: "#/components/schemas/PayoutConfiguration"
        created_at:
          type: string
          description: Timestamp when this payout was created.
          format: date-time
          nullable: true
        updated_at:
          type: string
          description: Timestamp when this payout was last updated.
          format: date-time
          nullable: true
        processed_at:
          type: string
          description: Timestamp when this payout was submitted to the payment network.
            Null until processed.
          format: date-time
          nullable: true
        effective_at:
          type: string
          description: Timestamp when funds were settled. Null until settlement is
            confirmed.
          format: date-time
          nullable: true
        status:
          $ref: "#/components/schemas/PaymentStatus"
        status_details:
          $ref: "#/components/schemas/PaymentStatusDetails"
        status_history:
          type: array
          items:
            $ref: "#/components/schemas/PaymentStatusHistory"
          description: Complete ordered history of all status changes for this payout.
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Key-value metadata stored with this payout.
          nullable: true
        funding_ids:
          type: array
          items:
            type: string
            format: uuid
          description: IDs of the funding events that included this payout.
        paykey:
          type: string
          description: Unmasked paykey token used for this payout.
        trace_ids:
          type: object
          additionalProperties:
            type: string
          description: Trace identifiers from the payment network. Keys depend on the
            payment rail.
        related_payments:
          type: array
          items:
            $ref: "#/components/schemas/RelatedPayment"
          description: Related payments and their relationship to this payout.
          nullable: true
        is_refund:
          type: boolean
          description: Whether this payout refunds an original charge.
        is_resubmit:
          type: boolean
          description: Whether this payout resubmits an original payout.
        has_resubmit:
          type: boolean
          description: Whether this payout has been resubmitted.
        documents:
          type: array
          items:
            $ref: "#/components/schemas/PaymentAuthorizationProof"
          description: Authorization documents for this payout, ordered by upload time.
          nullable: true
      additionalProperties: false
    UnmaskedPayoutResponse:
      required:
        - data
        - meta
        - response_type
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        data:
          $ref: "#/components/schemas/UnmaskedPayout"
      additionalProperties: false
    CancelPaykeyRequest:
      type: object
      properties:
        reason:
          type: string
          nullable: true
          description: Reason for canceling the paykey.
      additionalProperties: false
    AddressEvent:
      required:
        - address1
        - city
        - state
        - zip
      type: object
      properties:
        address1:
          type: string
          description: Primary address line, such as a street address or PO Box.
        address2:
          type: string
          description: Secondary address line, such as an apartment, suite, unit, or
            building.
          nullable: true
        city:
          type: string
          description: City, district, suburb, town, or village.
        state:
          type: string
          description: Two-letter state code.
        zip:
          type: string
          description: ZIP or postal code.
      additionalProperties: false
    ComplianceProfileEvent:
      type: object
      properties:
        dob:
          type: string
          description: Masked date of birth for an individual customer in `****-**-**`
            format.
          format: "****-**-**"
          nullable: true
        ssn:
          type: string
          description: Masked Social Security number for an individual customer in
            `***-**-****` format.
          format: "***-**-****"
          nullable: true
        ein:
          type: string
          description: Masked Employer Identification Number for a business customer in
            `**-*******` format.
          format: "**-*******"
          nullable: true
        legal_business_name:
          type: string
          description: Official registered name of the business customer.
          nullable: true
        website:
          type: string
          description: Official website URL for the business customer.
          nullable: true
      additionalProperties: false
    CustomerEvent:
      required:
        - created_at
        - device
        - email
        - id
        - name
        - phone
        - status
        - type
        - updated_at
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for the customer.
          format: uuid
        address:
          $ref: "#/components/schemas/AddressEvent"
        compliance_profile:
          $ref: "#/components/schemas/ComplianceProfileEvent"
        device:
          $ref: "#/components/schemas/MaskedCustomerDevice"
        name:
          type: string
          description: Full name for an individual customer or business name for a
            business customer.
        type:
          $ref: "#/components/schemas/CustomerType"
        email:
          type: string
          description: Customer email address.
        phone:
          type: string
          description: Customer phone number in E.164 format.
        external_id:
          type: string
          description: Unique identifier for the customer in your system.
          nullable: true
        status:
          $ref: "#/components/schemas/CustomerStatusEvent"
        created_at:
          type: string
          description: Timestamp of when the customer record was created.
          format: date-time
        updated_at:
          type: string
          description: Timestamp of the most recent update to the customer record.
          format: date-time
        metadata:
          type: object
          additionalProperties:
            type: string
            nullable: true
          description: Up to 20 user-defined key-value pairs associated with the customer.
          nullable: true
      additionalProperties: false
    CustomerStatusEvent:
      enum:
        - pending
        - review
        - verified
        - inactive
        - rejected
      type: string
    PaykeyBankDetailsEvent:
      required:
        - account_number
        - account_type
        - routing_number
      type: object
      properties:
        routing_number:
          type: string
          description: Bank routing number.
        account_number:
          type: string
          description: Masked bank account number.
        account_type:
          $ref: "#/components/schemas/AccountType"
      additionalProperties: false
    PaykeyEvent:
      required:
        - created_at
        - id
        - label
        - paykey
        - source
        - status
        - updated_at
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for the paykey.
          format: uuid
        customer_id:
          type: string
          description: Unique identifier for the customer associated with the paykey.
          format: uuid
          nullable: true
        label:
          type: string
          description: Human-readable label for the paykey.
        source:
          $ref: "#/components/schemas/PaykeySource"
        institution_name:
          type: string
          description: Name of the financial institution.
          nullable: true
        status:
          $ref: "#/components/schemas/PaykeyStatusEvent"
        status_details:
          $ref: "#/components/schemas/PaymentEventStatusDetails"
        expires_at:
          type: string
          description: Expiration date and time of the paykey, if applicable.
          format: date-time
          nullable: true
        created_at:
          type: string
          description: Timestamp of when the paykey was created.
          format: date-time
        updated_at:
          type: string
          description: Timestamp of the most recent update to the paykey.
          format: date-time
        paykey:
          type: string
          description: Full paykey value for creating payments. Store this value securely.
        bank_data:
          $ref: "#/components/schemas/PaykeyBankDetailsEvent"
        metadata:
          type: object
          additionalProperties:
            type: string
            nullable: true
          description: Up to 20 user-defined key-value pairs associated with the paykey.
          nullable: true
        balance:
          $ref: "#/components/schemas/PaykeyBalanceEventDetails"
      additionalProperties: false
    PaykeyStatusEvent:
      enum:
        - pending
        - active
        - inactive
        - rejected
        - review
        - blocked
      type: string
    PaymentEventStatusDetails:
      required:
        - message
        - reason
        - source
        - changed_at
        - code
      type: object
      properties:
        message:
          type: string
          description: A human-readable description of the current status.
        reason:
          $ref: "#/components/schemas/PaymentEventStatusReason"
        source:
          $ref: "#/components/schemas/PaymentStatusSource"
        code:
          type: string
          description: The status code if applicable.
          nullable: true
        changed_at:
          type: string
          description: The time the status change occurred.
          format: date-time
      additionalProperties: false
    ChargeConfigurationEvent:
      required:
        - balance_check
      type: object
      properties:
        balance_check:
          $ref: "#/components/schemas/BalanceCheckMode"
      additionalProperties: false
    ChargeEvent:
      required:
        - amount
        - config
        - consent_type
        - currency
        - description
        - device
        - id
        - paykey
        - payment_date
        - status
        - status_details
        - status_history
        - funding_ids
        - has_refund
        - has_resubmit
        - is_resubmit
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for this charge.
          format: uuid
        paykey:
          type: string
          description: The masked paykey token used for this charge.
        description:
          type: string
          description: A human-readable description of the charge.
          nullable: true
        payment_rail:
          $ref: "#/components/schemas/PaymentRailEvent"
        paykey_details:
          $ref: "#/components/schemas/PaykeyDetailsEvent"
        customer_details:
          $ref: "#/components/schemas/CustomerDetailsEvent"
        amount:
          type: integer
          description: Amount in cents.
          format: int32
        currency:
          type: string
          description: Currency code. Only `USD` is supported.
        payment_date:
          type: string
          description: Date when Straddle submits the charge for processing.
          format: date
        consent_type:
          $ref: "#/components/schemas/ConsentTypeEvent"
        device:
          $ref: "#/components/schemas/MaskedPaymentDevice"
        external_id:
          type: string
          description: Your unique identifier for this charge, used to correlate with your
            internal records.
          nullable: true
        config:
          $ref: "#/components/schemas/ChargeConfigurationEvent"
        created_at:
          type: string
          description: Timestamp when this charge was created.
          format: date-time
          nullable: true
        updated_at:
          type: string
          description: Timestamp when this charge was last updated.
          format: date-time
          nullable: true
        processed_at:
          type: string
          description: Timestamp when this charge was submitted to the payment network.
            Null until processed.
          format: date-time
          nullable: true
        effective_at:
          type: string
          description: Timestamp when funds were settled. Null until settlement is
            confirmed.
          format: date-time
          nullable: true
        status:
          $ref: "#/components/schemas/PaymentEventStatus"
        status_details:
          $ref: "#/components/schemas/PaymentEventStatusDetails"
        status_history:
          type: array
          items:
            $ref: "#/components/schemas/PaymentEventStatusHistory"
          description: Complete ordered history of all status changes for this charge.
        metadata:
          type: object
          additionalProperties:
            type: string
            nullable: true
          description: Key-value metadata stored with this charge.
          nullable: true
        funding_ids:
          type: array
          items:
            type: string
            format: uuid
          description: IDs of the funding events that included this charge.
        related_payments:
          type: array
          items:
            $ref: "#/components/schemas/RelatedPayment"
          description: Related payments and their relationship to this charge.
          nullable: true
          example:
            - id: 019e1bab-2ab4-751d-be80-f51c82c2ea64
              relationship: original
              payment_type: charge
        is_resubmit:
          type: boolean
          description: Whether this charge resubmits an original charge.
        has_resubmit:
          type: boolean
          description: Whether this charge has been resubmitted.
        has_refund:
          type: boolean
          description: Whether an associated payout has refunded this charge.
        documents:
          type: array
          items:
            $ref: "#/components/schemas/PaymentAuthorizationProof"
          description: Authorization documents for this charge, ordered by upload time.
          nullable: true
      additionalProperties: false
    ConsentTypeEvent:
      enum:
        - internet
        - signed
      type: string
    CustomerDetailsEvent:
      required:
        - customer_type
        - email
        - id
        - name
        - phone
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for the customer.
          format: uuid
        name:
          type: string
          description: Customer's full name or business name.
        email:
          type: string
          description: Customer's email address.
        phone:
          type: string
          description: Customer's phone number in E.164 format.
        customer_type:
          $ref: "#/components/schemas/CustomerType"
          description: Whether the customer is an individual or a business.
      additionalProperties: false
    PaykeyDetailsEvent:
      required:
        - customer_id
        - id
        - label
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for the paykey.
          format: uuid
        customer_id:
          type: string
          description: Unique identifier for the customer associated with the paykey.
          format: uuid
        label:
          type: string
          description: Display label combining the bank name and masked account number.
        balance:
          type: integer
          description: Available balance in cents when a balance check was performed. Null
            otherwise.
          format: int32
          nullable: true
      additionalProperties: false
    PaymentRailEvent:
      enum:
        - ach
      type: string
    PaymentEventStatus:
      enum:
        - created
        - scheduled
        - failed
        - cancelled
        - on_hold
        - pending
        - paid
        - reversed
      type: string
    PayoutConfigurationEvent:
      type: object
      additionalProperties: false
    PayoutEvent:
      required:
        - amount
        - config
        - consent_type
        - currency
        - description
        - device
        - id
        - paykey
        - payment_date
        - status
        - status_details
        - status_history
        - funding_ids
        - has_resubmit
        - is_refund
        - is_resubmit
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for this payout.
          format: uuid
        paykey:
          type: string
          description: The masked paykey token used for this payout.
        description:
          type: string
          description: A human-readable description of the payout.
          nullable: true
        payment_rail:
          $ref: "#/components/schemas/PaymentRailEvent"
        paykey_details:
          $ref: "#/components/schemas/PaykeyDetailsEvent"
        customer_details:
          $ref: "#/components/schemas/CustomerDetailsEvent"
        amount:
          type: integer
          description: Amount in cents.
          format: int32
        currency:
          type: string
          description: Currency code. Only `USD` is supported.
        payment_date:
          type: string
          description: Date when Straddle submits the payout for processing.
          format: date
        consent_type:
          $ref: "#/components/schemas/ConsentTypeEvent"
        device:
          $ref: "#/components/schemas/MaskedPaymentDevice"
        external_id:
          type: string
          description: Your unique identifier for this payout, used to correlate with your
            internal records.
          nullable: true
        config:
          $ref: "#/components/schemas/PayoutConfigurationEvent"
        created_at:
          type: string
          description: Timestamp when this payout was created.
          format: date-time
          nullable: true
        updated_at:
          type: string
          description: Timestamp when this payout was last updated.
          format: date-time
          nullable: true
        processed_at:
          type: string
          description: Timestamp when this payout was submitted to the payment network.
            Null until processed.
          format: date-time
          nullable: true
        effective_at:
          type: string
          description: Timestamp when funds were settled. Null until settlement is
            confirmed.
          format: date-time
          nullable: true
        status:
          $ref: "#/components/schemas/PaymentEventStatus"
        status_details:
          $ref: "#/components/schemas/PaymentEventStatusDetails"
        status_history:
          type: array
          items:
            $ref: "#/components/schemas/PaymentEventStatusHistory"
          description: Complete ordered history of all status changes for this payout.
        metadata:
          type: object
          additionalProperties:
            type: string
            nullable: true
          description: Key-value metadata stored with this payout.
          nullable: true
        funding_ids:
          type: array
          items:
            type: string
            format: uuid
          description: IDs of the funding events that included this payout.
        related_payments:
          type: array
          items:
            $ref: "#/components/schemas/RelatedPayment"
          description: Related payments and their relationship to this payout.
          nullable: true
          example:
            - id: 019e1bab-2ab4-751d-be80-f51c82c2ea64
              relationship: original
              payment_type: charge
        is_resubmit:
          type: boolean
          description: Whether this payout resubmits an original payout.
        has_resubmit:
          type: boolean
          description: Whether this payout has been resubmitted.
        is_refund:
          type: boolean
          description: Whether this payout refunds an original charge.
        documents:
          type: array
          items:
            $ref: "#/components/schemas/PaymentAuthorizationProof"
          description: Authorization documents for this payout, ordered by upload time.
          nullable: true
      additionalProperties: false
    PaymentEventStatusHistory:
      required:
        - changed_at
        - message
        - reason
        - source
        - status
      type: object
      properties:
        reason:
          $ref: "#/components/schemas/PaymentEventStatusReason"
        source:
          $ref: "#/components/schemas/PaymentStatusSource"
        message:
          type: string
          description: A human-readable description of the status.
        code:
          type: string
          description: The status code if applicable.
          nullable: true
        changed_at:
          type: string
          description: The time the status change occurred.
          format: date-time
        status:
          $ref: "#/components/schemas/PaymentEventStatus"
      additionalProperties: false
    PaymentEventStatusReason:
      enum:
        - insufficient_funds
        - closed_bank_account
        - invalid_bank_account
        - invalid_routing
        - disputed
        - payment_stopped
        - owner_deceased
        - frozen_bank_account
        - risk_review
        - fraudulent
        - duplicate_entry
        - invalid_paykey
        - payment_blocked
        - amount_too_large
        - too_many_attempts
        - internal_system_error
        - user_request
        - ok
        - other_network_return
        - payout_refused
        - validating
        - auto_hold
      type: string
    PaykeyBalanceRefreshStatus:
      enum:
        - pending
        - completed
        - failed
      type: string
    CreateQuilttPaykeyRequest:
      required:
        - customer_id
        - quiltt_token
      type: object
      properties:
        customer_id:
          type: string
          description: Unique identifier for the customer associated with the paykey.
          format: uuid
        quiltt_token:
          type: string
          description: Quiltt processor token generated by your application for use with
            the Straddle API.
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Up to 20 user-defined key-value pairs associated with the paykey.
          nullable: true
        config:
          $ref: "#/components/schemas/PaykeyConfiguration"
        external_id:
          type: string
          description: Unique identifier for the paykey in your system.
          nullable: true
      additionalProperties: false
    CustomerConfiguration:
      type: object
      properties:
        sandbox_outcome:
          $ref: "#/components/schemas/SimulatedCustomerOutcome"
        processing_method:
          $ref: "#/components/schemas/PaykeyProcessingMode"
      additionalProperties: false
    PaykeyBalanceDetails:
      required:
        - status
      type: object
      properties:
        account_balance:
          type: integer
          description: Most recently retrieved account balance in cents.
          format: int32
          nullable: true
        updated_at:
          type: string
          description: Timestamp of the most recent account balance update.
          format: date-time
          nullable: true
        status:
          $ref: "#/components/schemas/PaykeyBalanceRefreshStatus"
      additionalProperties: false
    PaykeyConfiguration:
      type: object
      properties:
        sandbox_outcome:
          $ref: "#/components/schemas/SimulatedPaykeyOutcome"
        processing_method:
          $ref: "#/components/schemas/PaykeyProcessingMode"
      additionalProperties: false
    SimulatedCustomerOutcome:
      enum:
        - standard
        - verified
        - rejected
        - review
      type: string
    SimulatedPaykeyOutcome:
      enum:
        - standard
        - active
        - rejected
        - review
      type: string
    PaykeyVerificationDecision:
      enum:
        - active
        - rejected
      type: string
    SimulatedPaymentOutcome:
      enum:
        - standard
        - paid
        - on_hold_daily_limit
        - cancelled_for_fraud_risk
        - cancelled_for_balance_check
        - failed_insufficient_funds
        - reversed_insufficient_funds
        - failed_customer_dispute
        - reversed_customer_dispute
        - failed_closed_bank_account
        - reversed_closed_bank_account
        - failed_not_authorized
        - reversed_not_authorized
      type: string
      description: Payment will simulate processing if not Standard.
    PaykeyProcessingMode:
      enum:
        - inline
        - background
        - skip
      type: string
    SetPaykeyVerificationDecisionRequest:
      required:
        - status
      type: object
      properties:
        status:
          $ref: "#/components/schemas/PaykeyVerificationDecision"
      additionalProperties: false
    PlatformAddress:
      type: object
      properties:
        line1:
          type: string
          nullable: true
          description: Primary street address.
        line2:
          type: string
          nullable: true
          description: Additional address information, such as a suite or unit.
        city:
          type: string
          nullable: true
          description: City for the address.
        state:
          type: string
          nullable: true
          description: State or region for the address.
        postal_code:
          type: string
          nullable: true
          description: Postal code for the address.
        country:
          type: string
          nullable: true
          description: Two-letter ISO 3166-1 country code.
      additionalProperties: false
    PlatformBusinessProfile:
      required:
        - name
        - website
      type: object
      properties:
        name:
          type: string
          description: Display name of the business.
        website:
          type: string
          description: URL of the business website.
        legal_name:
          type: string
          nullable: true
          description: Registered legal name of the business.
        description:
          type: string
          nullable: true
          description: Description of the business.
        use_case:
          type: string
          nullable: true
          description: Description of how the business uses Straddle.
        tax_id:
          type: string
          nullable: true
          description: Tax identification number of the business.
        phone:
          type: string
          nullable: true
          description: Primary phone number for the business.
        address:
          $ref: "#/components/schemas/PlatformAddress"
        industry:
          $ref: "#/components/schemas/PlatformIndustry"
        support_channels:
          $ref: "#/components/schemas/PlatformSupportChannels"
      additionalProperties: false
    PlatformIndustry:
      type: object
      properties:
        mcc:
          type: string
          nullable: true
          description: Merchant Category Code assigned to the business.
        sector:
          type: string
          nullable: true
          description: Industry sector of the business.
        category:
          type: string
          nullable: true
          description: Industry category of the business.
      additionalProperties: false
    Platform:
      required:
        - id
        - status
        - status_detail
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Unique identifier for the platform.
        status:
          enum:
            - created
            - onboarding
            - active
            - rejected
            - inactive
          type: string
          description: Current lifecycle status of the platform.
        status_detail:
          $ref: "#/components/schemas/PlatformStatusDetail"
        business_profile:
          $ref: "#/components/schemas/PlatformBusinessProfile"
        metadata:
          type: object
          additionalProperties:
            type: string
            nullable: true
          nullable: true
          description: Key-value metadata associated with the platform.
        external_id:
          type: string
          nullable: true
          description: Your unique identifier for the platform.
        created_at:
          type: string
          format: date-time
          nullable: true
          description: Timestamp when the platform was created.
        updated_at:
          type: string
          format: date-time
          nullable: true
          description: Timestamp when the platform was last updated.
      additionalProperties: false
    PlatformStatusDetail:
      required:
        - code
        - message
        - reason
        - source
      type: object
      properties:
        reason:
          enum:
            - unverified
            - new
            - in_review
            - pending
            - stuck
            - verified
            - failed_verification
            - disabled
            - terminated
          type: string
          description: Machine-readable reason for the current platform status.
        source:
          enum:
            - watchtower
          type: string
          description: Source that produced the current platform status.
        code:
          type: string
          description: Machine-readable code for the current platform status.
        message:
          type: string
          description: Human-readable explanation of the current platform status.
      additionalProperties: false
    PlatformSupportChannels:
      type: object
      properties:
        email:
          type: string
          nullable: true
          description: Customer support email address.
        phone:
          type: string
          nullable: true
          description: Customer support phone number.
        url:
          type: string
          nullable: true
          description: URL of the customer support page or contact form.
      additionalProperties: false
    User:
      required:
        - created_at
        - email
        - first_name
        - id
        - last_name
        - level
        - roles
        - status
        - updated_at
        - memberships
      type: object
      properties:
        id:
          type: string
          description: The unique identifier of the user.
          format: uuid
        organization_id:
          type: string
          description: The unique identifier of the organization this user belongs to.
          format: uuid
          nullable: true
        platform_id:
          type: string
          description: The unique identifier of the organization this user belongs to.
          format: uuid
          nullable: true
        authenticator_id:
          type: string
          description: The unique identifier used for authentication purposes.
          nullable: true
        status:
          enum:
            - invited
            - active
            - onboarding
            - inactive
          type: string
          description: The current status of the user.
        first_name:
          type: string
          description: The first name of the user.
        last_name:
          type: string
          description: The last name of the user.
        email:
          type: string
          description: The email address of the user.
        level:
          enum:
            - none
            - onboarding
            - straddle
            - platform
            - organization
          type: string
          description: The current status of the user.
        roles:
          type: array
          items:
            enum:
              - none
              - member
              - developer
              - admin
            type: string
          description: The role assigned to the user, determining their permissions within
            the system.
        created_at:
          type: string
          description: Timestamp of when the user was created.
          format: date-time
        updated_at:
          type: string
          description: Timestamp of the most recent update to the user.
          format: date-time
        memberships:
          type: array
          items:
            $ref: "#/components/schemas/UserMembership"
          description: Memberships that grant the user access to Straddle entities.
      additionalProperties: false
    AccountNameMatchDetails:
      required:
        - codes
        - decision
      type: object
      properties:
        names_on_account:
          type: array
          items:
            type: string
          nullable: true
          description: Account-holder names returned by the financial institution.
        matched_name:
          type: string
          nullable: true
          description: Account-holder name that matched the customer record.
        customer_name:
          type: string
          nullable: true
          description: Customer name evaluated during account verification.
        correlation_score:
          type: number
          format: double
          nullable: true
          description: Strength of the match between the customer name and account-holder
            names.
        decision:
          $ref: "#/components/schemas/PaykeyVerificationResult"
        reason:
          type: string
          nullable: true
          description: Reason for the name-match decision.
        codes:
          type: array
          items:
            type: string
          description: Result codes returned by the name-match check.
      additionalProperties: false
    AccountValidationDetails:
      required:
        - codes
        - decision
      type: object
      properties:
        decision:
          $ref: "#/components/schemas/PaykeyVerificationResult"
        reason:
          type: string
          nullable: true
          description: Reason for the account-validation decision.
        codes:
          type: array
          items:
            type: string
          description: Result codes returned by the account-validation check.
      additionalProperties: false
    PaykeyBalanceEventDetails:
      required:
        - status
      type: object
      properties:
        account_balance:
          type: number
          description: Most recently retrieved account balance in dollars.
          format: double
          nullable: true
        updated_at:
          type: string
          description: Timestamp of the most recent account balance update.
          format: date-time
          nullable: true
        status:
          $ref: "#/components/schemas/PaykeyBalanceRefreshStatus"
      additionalProperties: false
    PaykeyReview:
      required:
        - paykey_details
      type: object
      properties:
        paykey_details:
          $ref: "#/components/schemas/Paykey"
        verification_details:
          $ref: "#/components/schemas/PaykeyVerificationDetails"
      additionalProperties: false
    PaykeyReviewResponse:
      required:
        - data
        - meta
        - response_type
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        data:
          $ref: "#/components/schemas/PaykeyReview"
      additionalProperties: false
    PaykeyVerificationBreakdown:
      type: object
      properties:
        name_match:
          $ref: "#/components/schemas/AccountNameMatchDetails"
        account_validation:
          $ref: "#/components/schemas/AccountValidationDetails"
      additionalProperties: false
    PaykeyVerificationResult:
      enum:
        - accept
        - reject
        - review
      type: string
    PaykeyVerificationDetails:
      required:
        - breakdown
        - created_at
        - decision
        - id
        - messages
        - updated_at
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for the verification details.
          format: uuid
        decision:
          $ref: "#/components/schemas/PaykeyVerificationResult"
        messages:
          type: object
          additionalProperties:
            type: string
          description: Messages returned by the paykey verification process.
        breakdown:
          $ref: "#/components/schemas/PaykeyVerificationBreakdown"
        created_at:
          type: string
          description: Timestamp of when the verification was initiated.
          format: date-time
        updated_at:
          type: string
          description: Timestamp of the most recent update to the verification details.
          format: date-time
      additionalProperties: false
    FundingEventTransferDirection:
      enum:
        - deposit
        - withdrawal
      type: string
      description: Transfer direction relative to the linked bank account. `deposit`
        moves funds into the account, and `withdrawal` moves funds out.
    FundingEvent:
      required:
        - amount
        - created_at
        - direction
        - event_type
        - id
        - payment_count
        - trace_numbers
        - transfer_date
        - updated_at
        - status_history
        - trace_ids
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for this funding event.
          format: uuid
        amount:
          type: integer
          description: Total funding event amount in the smallest currency unit. For
            example, `1000` is $10.00 USD.
          format: int32
        direction:
          $ref: "#/components/schemas/FundingEventTransferDirection"
        event_type:
          $ref: "#/components/schemas/FundingEventType"
        trace_numbers:
          type: array
          items:
            type: string
          description: Network trace numbers associated with payments in this funding event.
        payment_count:
          type: integer
          description: Number of payments included in this funding event.
          format: int32
        transfer_date:
          type: string
          description: The date the funds transfer was initiated.
          format: date
        created_at:
          type: string
          description: Timestamp when this funding event was created.
          format: date-time
        updated_at:
          type: string
          description: Timestamp when this funding event was last updated.
          format: date-time
        trace_ids:
          type: object
          additionalProperties:
            type: string
          description: Network-level trace identifiers assigned during processing. Keys
            vary by payment rail.
        status:
          $ref: "#/components/schemas/PaymentStatus"
          description: Current status of this funding event.
        status_details:
          $ref: "#/components/schemas/PaymentStatusDetails"
          description: Reason, source, and message for the most recent status change.
        status_history:
          type: array
          items:
            $ref: "#/components/schemas/PaymentStatusHistory"
          description: Complete ordered history of all status changes for this funding
            event.
        config:
          $ref: "#/components/schemas/FundingEventConfiguration"
          description: Configuration used to process this funding event.
        linked_bank_account_details:
          $ref: "#/components/schemas/UnmaskedLinkedBankAccountDetails"
          description: Details of the linked bank account used for this funding event.
      additionalProperties: false
    SimulateFundingEventRequest:
      required:
        - funding_event_job_type
      type: object
      properties:
        funding_event_job_type:
          $ref: "#/components/schemas/FundingEventSimulationType"
          description: Required. Selects charge or payout activity for the simulated
            funding event.
        sandbox_outcome:
          $ref: "#/components/schemas/SimulatedPaymentOutcome"
          description: Optional. Sets the processing outcome for the simulated funding
            event. Defaults to `standard`.
      additionalProperties: false
    FundingEventSimulationResult:
      required:
        - id
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for the created funding event.
          format: uuid
      additionalProperties: false
    FundingEventSimulationResponse:
      required:
        - data
        - meta
        - response_type
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        data:
          $ref: "#/components/schemas/FundingEventSimulationResult"
      additionalProperties: false
    FundingEventConfiguration:
      type: object
      properties:
        sandbox_outcome:
          $ref: "#/components/schemas/SimulatedPaymentOutcome"
          description: Processing outcome configured for this simulated funding event.
      additionalProperties: false
    FundingEventEvent:
      required:
        - amount
        - created_at
        - direction
        - event_type
        - id
        - payment_count
        - status_history
        - trace_ids
        - transfer_date
        - updated_at
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for this funding event.
          format: uuid
        amount:
          type: integer
          description: Total funding event amount in the smallest currency unit. For
            example, `1000` is $10.00 USD.
          format: int32
        direction:
          $ref: "#/components/schemas/FundingEventTransferDirection"
        event_type:
          $ref: "#/components/schemas/FundingEventType"
        trace_ids:
          type: object
          additionalProperties:
            type: string
          description: Network-level trace identifiers assigned during processing. Keys
            vary by payment rail.
        payment_count:
          type: integer
          description: Number of payments included in this funding event.
          format: int32
        transfer_date:
          type: string
          description: The date the funds transfer was initiated.
          format: date
        created_at:
          type: string
          description: Timestamp when this funding event was created.
          format: date-time
        updated_at:
          type: string
          description: Timestamp when this funding event was last updated.
          format: date-time
        status:
          $ref: "#/components/schemas/PaymentEventStatus"
          description: Current status of this funding event.
        status_details:
          $ref: "#/components/schemas/PaymentEventStatusDetails"
          description: Reason, source, and message for the most recent status change.
        status_history:
          type: array
          items:
            $ref: "#/components/schemas/PaymentEventStatusHistory"
          description: Complete ordered history of all status changes for this funding
            event.
      additionalProperties: false
    FundingEventSimulationType:
      enum:
        - charges
        - payouts
      type: string
      description: Supported funding event categories are `charges` and `payouts`.
    FundingEventResponse:
      required:
        - data
        - meta
        - response_type
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        data:
          $ref: "#/components/schemas/FundingEvent"
      additionalProperties: false
    UnblockPaykeyRequest:
      type: object
      properties:
        message:
          type: string
          description: Optional message describing the reason for unblocking.
          nullable: true
      additionalProperties: false
    AccountSettings:
      required:
        - charges
        - configuration
        - consent_types
        - customer_types
        - payment_types
        - payouts
        - statement_settings
      type: object
      properties:
        charges:
          $ref: "#/components/schemas/ChargeSettings"
        payouts:
          $ref: "#/components/schemas/PayoutSettings"
        statement_settings:
          $ref: "#/components/schemas/AccountStatementSettings"
        payment_types:
          $ref: "#/components/schemas/AccountPaymentTypeSettings"
        customer_types:
          $ref: "#/components/schemas/AccountCustomerTypeSettings"
        consent_types:
          $ref: "#/components/schemas/AccountConsentSettings"
        configuration:
          $ref: "#/components/schemas/AccountPolicyControls"
      additionalProperties: false
    ChargeSettings:
      required:
        - daily_amount
        - max_amount
        - monthly_amount
        - monthly_count
      type: object
      properties:
        max_amount:
          type: integer
          format: int64
          description: Maximum amount in cents for one charge.
        monthly_amount:
          type: integer
          format: int64
          description: Monthly charge amount limit in cents.
        daily_amount:
          type: integer
          format: int64
          description: Daily charge amount limit in cents.
        monthly_count:
          type: integer
          format: int64
          description: Maximum number of charges per calendar month.
        funding_time:
          type: string
          nullable: true
          description: Funding schedule applied to charges.
        linked_bank_account_id:
          type: string
          nullable: true
          description: ID of the linked bank account used for charge settlement.
      additionalProperties: false
    AccountPolicyControls:
      required:
        - allow_customer_identity_skip
        - allow_data_unmask
        - allow_duplicate_email
        - allow_paykey_verification_skip
      type: object
      properties:
        allow_data_unmask:
          type: boolean
          description: Whether the account can retrieve unmasked sensitive fields.
        allow_duplicate_email:
          type: boolean
          description: Whether multiple customers can share one email address.
        allow_customer_identity_skip:
          type: boolean
          description: Whether customer identity verification can be skipped.
        allow_paykey_verification_skip:
          type: boolean
          description: Whether paykey verification can be skipped.
      additionalProperties: false
    AccountConsentSettings:
      required:
        - internet
        - signed_agreement
      type: object
      properties:
        internet:
          enum:
            - active
            - inactive
          type: string
          description: Status of internet authorization support for the account.
        signed_agreement:
          enum:
            - active
            - inactive
          type: string
          description: Status of signed-agreement authorization support for the account.
      additionalProperties: false
    AccountCustomerTypeSettings:
      required:
        - businesses
        - individuals
      type: object
      properties:
        individuals:
          enum:
            - active
            - inactive
          type: string
          description: Status of individual-customer support for the account.
        businesses:
          enum:
            - active
            - inactive
          type: string
          description: Status of business-customer support for the account.
      additionalProperties: false
    AccountSettingsResponse:
      required:
        - data
        - meta
        - response_type
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMetadata"
        response_type:
          enum:
            - object
            - array
            - error
            - none
          type: string
          description: |-
            Indicates how the response content is structured.
            - `object` means `data` contains one JSON object.
            - `array` means `data` contains an array of objects.
            - `error` means `error` contains error details.
            - `none` means the response has no data.
        data:
          $ref: "#/components/schemas/AccountSettings"
      additionalProperties: false
    AccountPaymentTypeSettings:
      required:
        - charges
        - payouts
      type: object
      properties:
        charges:
          enum:
            - active
            - inactive
          type: string
          description: Status of charge support for the account.
        payouts:
          enum:
            - active
            - inactive
          type: string
          description: Status of payout support for the account.
      additionalProperties: false
    PayoutSettings:
      required:
        - daily_amount
        - max_amount
        - monthly_amount
        - monthly_count
      type: object
      properties:
        max_amount:
          type: integer
          format: int64
          description: Maximum amount in cents for one payout.
        monthly_amount:
          type: integer
          format: int64
          description: Monthly payout amount limit in cents.
        daily_amount:
          type: integer
          format: int64
          description: Daily payout amount limit in cents.
        monthly_count:
          type: integer
          format: int64
          description: Maximum number of payouts per calendar month.
        funding_time:
          type: string
          nullable: true
          description: Funding schedule applied to payouts.
        linked_bank_account_id:
          type: string
          nullable: true
          description: ID of the linked bank account used for payout settlement.
      additionalProperties: false
    AccountStatementSettings:
      type: object
      properties:
        company_name:
          type: string
          nullable: true
          description: Company name used in statement records.
        company_id:
          type: string
          nullable: true
          description: Company identifier used in ACH records.
        default_descriptor:
          type: string
          nullable: true
          description: Default descriptor for account payments.
      additionalProperties: false
    UserMembership:
      required:
        - authenticator_organization_id
        - entity_name
        - level
        - roles
      type: object
      properties:
        level:
          enum:
            - none
            - onboarding
            - account
            - organization
            - platform
            - straddle
          type: string
          description: Entity level at which the membership applies.
        entity_id:
          type: string
          format: uuid
          nullable: true
          description: Unique identifier of the entity associated with the membership.
        entity_name:
          type: string
          description: Display name of the entity associated with the membership.
        roles:
          type: array
          items:
            enum:
              - none
              - member
              - developer
              - admin
            type: string
          description: Roles granted by the membership.
        authenticator_organization_id:
          type: string
          description: Organization identifier used by the authentication provider.
      additionalProperties: false
    FundingEventPaymentReason:
      enum:
        - credit
        - debit
        - reversal
        - failed
      type: string
      description: Reason the payment was included in the funding event.
    FundingEventPaymentSortBy:
      enum:
        - created_at
        - payment_date
        - effective_at
        - id
      type: string
      description: Field used to sort funding event payments.
    FundingEventPayment:
      required:
        - currency
        - external_id
        - funding_amount
        - id
        - payment_amount
        - payment_date
        - payment_type
        - reason
        - status
        - trace_ids
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for this payment.
          format: uuid
        payment_type:
          $ref: "#/components/schemas/PaymentType"
          description: Whether this payment is a charge or payout.
        payment_date:
          type: string
          description: The date on which this payment was submitted for processing.
          format: date
        currency:
          type: string
          description: Three-letter ISO 4217 currency code.
        funding_amount:
          type: integer
          description: Portion of the payment amount included in this funding event, in
            the smallest currency unit.
          format: int32
        reason:
          $ref: "#/components/schemas/FundingEventPaymentReason"
          description: Reason this payment was included in the funding event.
        payment_amount:
          type: integer
          description: Total payment amount in the smallest currency unit (e.g. 1000 =
            $10.00 USD).
          format: int32
        customer_details:
          $ref: "#/components/schemas/CustomerDetails"
          description: Details of the customer associated with this payment.
        paykey_details:
          $ref: "#/components/schemas/PaykeyDetails"
          description: Details of the paykey used for this payment.
        status:
          $ref: "#/components/schemas/PaymentStatus"
          description: Current status of this payment.
        external_id:
          type: string
          description: Your unique identifier for this payment, used to correlate with
            your internal records.
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Key-value metadata for this payment. Included only when
            `include_metadata` is `true`.
          nullable: true
        trace_ids:
          type: object
          additionalProperties:
            type: string
          description: Network-level trace identifiers assigned during processing. Keys
            vary by payment rail.
      additionalProperties: false
    FundingEventPaymentListResponse:
      required:
        - data
        - meta
        - response_type
      type: object
      properties:
        meta:
          $ref: "#/components/schemas/PageMetadata"
        response_type:
          $ref: "#/components/schemas/ResponseType"
        data:
          type: array
          items:
            $ref: "#/components/schemas/FundingEventPayment"
          description: Funding-event payments returned for this page.
      additionalProperties: false
    RefundChargeRequest:
      required:
        - amount
      type: object
      properties:
        amount:
          type: integer
          description: Refund amount in cents. `null` refunds the full original amount. A
            value must be greater than zero and no more than the original charge
            amount.
          format: int32
          example: 5000
          nullable: true
        description:
          type: string
          description: Description for the refund payout. Defaults to a description that
            identifies the original charge.
          nullable: true
        external_id:
          type: string
          description: Your unique identifier for the refund. Defaults to a new value if
            omitted.
          nullable: true
        payment_date:
          type: string
          description: Date when Straddle submits the refund payout for processing.
            Defaults to today if omitted.
          format: date
          nullable: true
        metadata:
          type: object
          additionalProperties:
            type: string
          description: User-defined string key-value pairs for the refund payout.
          nullable: true
      additionalProperties: false
    PaymentRelationship:
      enum:
        - original
        - resubmit
        - refund
      type: string
    RelatedPayment:
      required:
        - id
        - payment_type
        - relationship
      type: object
      properties:
        id:
          type: string
          description: Unique identifier of the related payment.
          format: uuid
        relationship:
          $ref: "#/components/schemas/PaymentRelationship"
        payment_type:
          $ref: "#/components/schemas/PaymentType"
      additionalProperties: false
    ResubmitChargeRequest:
      type: object
      properties:
        description:
          type: string
          description: Description for the resubmitted charge. Defaults to the original
            description if omitted.
          nullable: true
        payment_date:
          type: string
          description: Date when Straddle submits the resubmitted charge for processing.
            Defaults to today if omitted.
          format: date
          nullable: true
        external_id:
          type: string
          description: Your unique identifier for the resubmitted charge. Defaults to a
            new value if omitted.
          nullable: true
      additionalProperties: false
    ResubmitPayoutRequest:
      type: object
      properties:
        description:
          type: string
          description: Description for the resubmitted payout. Defaults to the original
            description if omitted.
          nullable: true
        payment_date:
          type: string
          description: Date when Straddle submits the resubmitted payout for processing.
            Defaults to today if omitted.
          format: date
          nullable: true
        external_id:
          type: string
          description: Your unique identifier for the resubmitted payout. Defaults to a
            new value if omitted.
          nullable: true
      additionalProperties: false
    PaymentDocumentType:
      enum:
        - payment_authorization
      type: string
    PaymentAuthorizationProof:
      required:
        - document_id
        - document_name
        - document_size
        - document_type
        - uploaded_at
      type: object
      properties:
        document_id:
          type: string
          description: Unique identifier for this document.
          format: uuid
        document_name:
          type: string
          description: The file name of this document as uploaded.
        document_type:
          $ref: "#/components/schemas/PaymentDocumentType"
        document_size:
          type: integer
          description: The size of this document in bytes.
          format: int64
        uploaded_at:
          type: string
          description: The UTC timestamp when this document was uploaded.
          format: date-time
      additionalProperties: false
  securitySchemes:
    Bearer:
      type: http
      description: Send the API key as a bearer token in the `Authorization` header.
      scheme: bearer
      bearerFormat: JWT
  parameters:
    RequestId:
      name: Request-Id
      in: header
      description: Optional client-generated identifier for tracing one request.
      schema:
        type: string
    CorrelationId:
      name: Correlation-Id
      in: header
      description: Optional client-generated identifier for tracing a series of
        related requests.
      schema:
        type: string
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      description: Optional client-generated key for an idempotent request.
      schema:
        maxLength: 40
        minLength: 10
        type: string
    StraddleAccountId:
      name: Straddle-Account-Id
      in: header
      description: For platform requests, the embedded account UUID that sets the
        request scope.
      schema:
        type: string
        format: uuid
webhooks:
  account.created.v1:
    post:
      operationId: account.created.v1
      description: Straddle sends this event when a platform creates an account. The
        `data` field contains the account.
      summary: "Webhook event: account.created.v1"
      x-svix-group-name: Accounts
      requestBody:
        description: Payload for the `account.created.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: account.created.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/Account"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
      x-svix-feature-flags:
        - platform
  account.event.v1:
    post:
      operationId: account.event.v1
      description: Straddle sends this event when an account changes, including
        updates to its profile, status, settings, limits, or capabilities. The
        `data` field contains the updated account.
      summary: "Webhook event: account.event.v1"
      x-svix-group-name: Accounts
      requestBody:
        description: Payload for the `account.event.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: account.event.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/Account"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
      x-svix-feature-flags:
        - platform
  representative.event.v1:
    post:
      operationId: representative.event.v1
      description: Straddle sends this event when a representative's status or fields
        change. The `data` field contains the updated representative.
      summary: "Webhook event: representative.event.v1"
      x-svix-group-name: Representatives
      requestBody:
        description: Payload for the `representative.event.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: representative.event.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/Representative"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
      x-svix-feature-flags:
        - platform
  representative.created.v1:
    post:
      operationId: representative.created.v1
      description: Straddle sends this event when a platform creates a representative.
        The `data` field contains the representative.
      summary: "Webhook event: representative.created.v1"
      x-svix-group-name: Representatives
      requestBody:
        description: Payload for the `representative.created.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: representative.created.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/Representative"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
      x-svix-feature-flags:
        - platform
  linked_bank_account.event.v1:
    post:
      operationId: linked_bank_account.event.v1
      description: Straddle sends this event when a linked bank account's status or
        fields change. The `data` field contains the updated linked bank
        account.
      summary: "Webhook event: linked_bank_account.event.v1"
      x-svix-group-name: LinkedBankAccounts
      requestBody:
        description: Payload for the `linked_bank_account.event.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: linked_bank_account.event.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/LinkedBankAccount"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
      x-svix-feature-flags:
        - platform
  linked_bank_account.created.v1:
    post:
      operationId: linked_bank_account.created.v1
      description: Straddle sends this event when a platform creates a linked bank
        account. The `data` field contains the linked bank account.
      summary: "Webhook event: linked_bank_account.created.v1"
      x-svix-group-name: LinkedBankAccounts
      requestBody:
        description: Payload for the `linked_bank_account.created.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: linked_bank_account.created.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/LinkedBankAccount"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
      x-svix-feature-flags:
        - platform
  capability_request.event.v1:
    post:
      operationId: capability_request.event.v1
      description: Straddle sends this event when a capability request's status or
        fields change. The `data` field contains the updated capability request.
      summary: "Webhook event: capability_request.event.v1"
      x-svix-group-name: CapabilityRequests
      requestBody:
        description: Payload for the `capability_request.event.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: capability_request.event.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/CapabilityRequest"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
      x-svix-feature-flags:
        - platform
  capability_request.created.v1:
    post:
      operationId: capability_request.created.v1
      description: Straddle sends this event when a platform creates a capability
        request. The `data` field contains the capability request.
      summary: "Webhook event: capability_request.created.v1"
      x-svix-group-name: CapabilityRequests
      requestBody:
        description: Payload for the `capability_request.created.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: capability_request.created.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/CapabilityRequest"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
      x-svix-feature-flags:
        - platform
  customer.event.v1:
    post:
      operationId: customer.event.v1
      description: Reports changes to a customer's profile or status, including
        verification, compliance, and risk updates.
      summary: "Webhook event: customer.event.v1"
      x-svix-group-name: Customers
      requestBody:
        description: Payload for the `customer.event.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: customer.event.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/CustomerEvent"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
  customer.created.v1:
    post:
      operationId: customer.created.v1
      description: Reports the creation of a customer. The payload includes the
        customer profile and initial verification status.
      summary: "Webhook event: customer.created.v1"
      x-svix-group-name: Customers
      requestBody:
        description: Payload for the `customer.created.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: customer.created.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/CustomerEvent"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
  paykey.event.v1:
    post:
      operationId: paykey.event.v1
      description: Reports changes to a paykey's status, verification result, balance,
        or bank account details.
      summary: "Webhook event: paykey.event.v1"
      x-svix-group-name: Customers
      requestBody:
        description: Payload for the `paykey.event.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: paykey.event.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/PaykeyEvent"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
  paykey.created.v1:
    post:
      operationId: paykey.created.v1
      description: Reports the creation of a paykey that links a customer to a bank
        account. The payload includes the paykey's status and bank account
        details.
      summary: "Webhook event: paykey.created.v1"
      x-svix-group-name: Customers
      requestBody:
        description: Payload for the `paykey.created.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: paykey.created.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/PaykeyEvent"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
  charge.created.v1:
    post:
      operationId: charge.created.v1
      description: Sent when a charge is created. The payload contains the charge's
        current details.
      summary: "Webhook event: charge.created.v1"
      x-svix-group-name: Payments
      requestBody:
        description: Payload for the `charge.created.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: charge.created.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/ChargeEvent"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
  charge.event.v1:
    post:
      operationId: charge.event.v1
      description: Sent when a charge is created or its status changes. The payload
        contains the charge's current details.
      summary: "Webhook event: charge.event.v1"
      x-svix-group-name: Payments
      requestBody:
        description: Payload for the `charge.event.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: charge.event.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/ChargeEvent"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
  payout.created.v1:
    post:
      operationId: payout.created.v1
      description: Sent when a payout is created. The payload contains the payout's
        current details.
      summary: "Webhook event: payout.created.v1"
      x-svix-group-name: Payments
      requestBody:
        description: Payload for the `payout.created.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: payout.created.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/PayoutEvent"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
  payout.event.v1:
    post:
      operationId: payout.event.v1
      description: Sent when a payout is created or its status changes. The payload
        contains the payout's current details.
      summary: "Webhook event: payout.event.v1"
      x-svix-group-name: Payments
      requestBody:
        description: Payload for the `payout.event.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: payout.event.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/PayoutEvent"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
  platform.event.v1:
    post:
      operationId: platform.event.v1
      description: Fired when a platform is created. This webhook is only supposed to
        be consumed by Straddle.
      summary: "Webhook event: platform.event.v1"
      x-svix-feature-flags:
        - internal
      x-svix-group-name: Platforms
      requestBody:
        description: Payload for the `platform.event.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: platform.event.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/Platform"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
  platform.created.v1:
    post:
      operationId: platform.created.v1
      description: Triggered when any significant change occurs to a platform. This
        webhook is only supposed to be consumed by Straddle.
      summary: "Webhook event: platform.created.v1"
      x-svix-feature-flags:
        - internal
      x-svix-group-name: Platforms
      requestBody:
        description: Payload for the `platform.created.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: platform.created.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/Platform"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
  user.event.v1:
    post:
      operationId: user.event.v1
      description: Fired when a user is created. This webhook is only supposed to be
        consumed by Straddle.
      summary: "Webhook event: user.event.v1"
      x-svix-feature-flags:
        - internal
      x-svix-group-name: Users
      requestBody:
        description: Payload for the `user.event.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: user.event.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/User"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
  user.created.v1:
    post:
      operationId: user.created.v1
      description: Triggered when any significant change occurs to a user. This
        webhook is only supposed to be consumed by Straddle.
      summary: "Webhook event: user.created.v1"
      x-svix-feature-flags:
        - internal
      x-svix-group-name: Users
      requestBody:
        description: Payload for the `user.created.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: user.created.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/User"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
  funding_event.created.v1:
    post:
      operationId: funding_event.created.v1
      description: Straddle sends this webhook when it creates a funding event. The
        payload includes the amount, transfer direction, event type, and initial
        status.
      summary: "Webhook event: funding_event.created.v1"
      x-svix-group-name: FundingEvents
      requestBody:
        description: Payload for the `funding_event.created.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: funding_event.created.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/FundingEventEvent"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
  funding_event.event.v1:
    post:
      operationId: funding_event.event.v1
      description: Straddle sends this webhook when it creates or updates a funding
        event. The payload includes the current status and complete status
        history.
      summary: "Webhook event: funding_event.event.v1"
      x-svix-group-name: FundingEvents
      requestBody:
        description: Payload for the `funding_event.event.v1` event.
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type:
                  type: string
                  description: Type of this event.
                  example: funding_event.event.v1
                event_id:
                  type: string
                  format: uuid
                  description: Unique identifier for this event.
                  example: 85df84c7-a298-44bc-8040-7bd2ae372cbf
                account_id:
                  type: string
                  format: uuid
                  description: Unique identifier for the account associated with this event.
                  example: 857c679a-8e80-47f1-9334-39dd08ea801f
                data:
                  $ref: "#/components/schemas/FundingEventEvent"
              required:
                - event_type
                - event_id
                - account_id
                - data
      responses:
        "200":
          description: Webhook received.
